# This file is generated by the BAML compiler.
# Do not edit this file directly.
# Instead, edit the BAML files and recompile.

# ruff: noqa: E501,F401
# flake8: noqa: E501,F401
# pylint: disable=unused-import,line-too-long
# fmt: off

from ..types.classes.cls_linteroutput import LinterOutput
from ..types.partial.classes.cls_linteroutput import PartialLinterOutput
from baml_core.stream import AsyncStream
from typing import Callable, List, Protocol, runtime_checkable


import typing

import pytest
from contextlib import contextmanager
from unittest import mock

ImplName = typing.Literal["version1"]

T = typing.TypeVar("T", bound=typing.Callable[..., typing.Any])
CLS = typing.TypeVar("CLS", bound=type)


INoTippingOutput = List[LinterOutput]

@runtime_checkable
class INoTipping(Protocol):
    """
    This is the interface for a function.

    Args:
        arg: str

    Returns:
        List[LinterOutput]
    """

    async def __call__(self, arg: str, /) -> List[LinterOutput]:
        ...

   

@runtime_checkable
class INoTippingStream(Protocol):
    """
    This is the interface for a stream function.

    Args:
        arg: str

    Returns:
        AsyncStream[List[LinterOutput], List[LinterOutput]]
    """

    def __call__(self, arg: str, /) -> AsyncStream[List[LinterOutput], List[LinterOutput]]:
        ...
class BAMLNoTippingImpl:
    async def run(self, arg: str, /) -> List[LinterOutput]:
        ...
    
    def stream(self, arg: str, /) -> AsyncStream[List[LinterOutput], List[LinterOutput]]:
        ...

class IBAMLNoTipping:
    def register_impl(
        self, name: ImplName
    ) -> typing.Callable[[INoTipping, INoTippingStream], None]:
        ...

    async def __call__(self, arg: str, /) -> List[LinterOutput]:
        ...

    def stream(self, arg: str, /) -> AsyncStream[List[LinterOutput], List[LinterOutput]]:
        ...

    def get_impl(self, name: ImplName) -> BAMLNoTippingImpl:
        ...

    @contextmanager
    def mock(self) -> typing.Generator[mock.AsyncMock, None, None]:
        """
        Utility for mocking the NoTippingInterface.

        Usage:
            ```python
            # All implementations are mocked.

            async def test_logic() -> None:
                with baml.NoTipping.mock() as mocked:
                    mocked.return_value = ...
                    result = await NoTippingImpl(...)
                    assert mocked.called
            ```
        """
        ...

    @typing.overload
    def test(self, test_function: T) -> T:
        """
        Provides a pytest.mark.parametrize decorator to facilitate testing different implementations of
        the NoTippingInterface.

        Args:
            test_function : T
                The test function to be decorated.

        Usage:
            ```python
            # All implementations will be tested.

            @baml.NoTipping.test
            async def test_logic(NoTippingImpl: INoTipping) -> None:
                result = await NoTippingImpl(...)
            ```
        """
        ...

    @typing.overload
    def test(self, *, exclude_impl: typing.Iterable[ImplName] = [], stream: bool = False) -> pytest.MarkDecorator:
        """
        Provides a pytest.mark.parametrize decorator to facilitate testing different implementations of
        the NoTippingInterface.

        Args:
            exclude_impl : Iterable[ImplName]
                The names of the implementations to exclude from testing.
            stream: bool
                If set, will return a streamable version of the test function.

        Usage:
            ```python
            # All implementations except the given impl will be tested.

            @baml.NoTipping.test(exclude_impl=["implname"])
            async def test_logic(NoTippingImpl: INoTipping) -> None:
                result = await NoTippingImpl(...)
            ```

            ```python
            # Streamable version of the test function.

            @baml.NoTipping.test(stream=True)
            async def test_logic(NoTippingImpl: INoTippingStream) -> None:
                async for result in NoTippingImpl(...):
                    ...
            ```
        """
        ...

    @typing.overload
    def test(self, test_class: typing.Type[CLS]) -> typing.Type[CLS]:
        """
        Provides a pytest.mark.parametrize decorator to facilitate testing different implementations of
        the NoTippingInterface.

        Args:
            test_class : Type[CLS]
                The test class to be decorated.

        Usage:
        ```python
        # All implementations will be tested in every test method.

        @baml.NoTipping.test
        class TestClass:
            def test_a(self, NoTippingImpl: INoTipping) -> None:
                ...
            def test_b(self, NoTippingImpl: INoTipping) -> None:
                ...
        ```
        """
        ...

BAMLNoTipping: IBAMLNoTipping
