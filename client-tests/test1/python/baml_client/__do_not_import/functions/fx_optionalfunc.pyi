# This file is generated by the BAML compiler.
# Do not edit this file directly.
# Instead, edit the BAML files and recompile.

# ruff: noqa: E501,F401
# flake8: noqa: E501,F401
# pylint: disable=unused-import,line-too-long
# fmt: off

from ..types.classes.cls_optionalobject import OptionalObject
from ..types.partial.classes.cls_optionalobject import PartialOptionalObject
from baml_core.stream import AsyncStream
from typing import Callable, Protocol, runtime_checkable


import typing

import pytest
from contextlib import contextmanager
from unittest import mock

ImplName = type(None)

T = typing.TypeVar("T", bound=typing.Callable[..., typing.Any])
CLS = typing.TypeVar("CLS", bound=type)


IOptionalFuncOutput = str

@runtime_checkable
class IOptionalFunc(Protocol):
    """
    This is the interface for a function.

    Args:
        arg: OptionalObject

    Returns:
        str
    """

    async def __call__(self, arg: OptionalObject, /) -> str:
        ...

   

@runtime_checkable
class IOptionalFuncStream(Protocol):
    """
    This is the interface for a stream function.

    Args:
        arg: OptionalObject

    Returns:
        AsyncStream[str, str]
    """

    def __call__(self, arg: OptionalObject, /) -> AsyncStream[str, str]:
        ...
class BAMLOptionalFuncImpl:
    async def run(self, arg: OptionalObject, /) -> str:
        ...
    
    def stream(self, arg: OptionalObject, /) -> AsyncStream[str, str]:
        ...

class IBAMLOptionalFunc:
    def register_impl(
        self, name: ImplName
    ) -> typing.Callable[[IOptionalFunc, IOptionalFuncStream], None]:
        ...

    async def __call__(self, arg: OptionalObject, /) -> str:
        ...

    def stream(self, arg: OptionalObject, /) -> AsyncStream[str, str]:
        ...

    def get_impl(self, name: ImplName) -> BAMLOptionalFuncImpl:
        ...

    @contextmanager
    def mock(self) -> typing.Generator[mock.AsyncMock, None, None]:
        """
        Utility for mocking the OptionalFuncInterface.

        Usage:
            ```python
            # All implementations are mocked.

            async def test_logic() -> None:
                with baml.OptionalFunc.mock() as mocked:
                    mocked.return_value = ...
                    result = await OptionalFuncImpl(...)
                    assert mocked.called
            ```
        """
        ...

    @typing.overload
    def test(self, test_function: T) -> T:
        """
        Provides a pytest.mark.parametrize decorator to facilitate testing different implementations of
        the OptionalFuncInterface.

        Args:
            test_function : T
                The test function to be decorated.

        Usage:
            ```python
            # All implementations will be tested.

            @baml.OptionalFunc.test
            async def test_logic(OptionalFuncImpl: IOptionalFunc) -> None:
                result = await OptionalFuncImpl(...)
            ```
        """
        ...

    @typing.overload
    def test(self, *, exclude_impl: typing.Iterable[ImplName] = [], stream: bool = False) -> pytest.MarkDecorator:
        """
        Provides a pytest.mark.parametrize decorator to facilitate testing different implementations of
        the OptionalFuncInterface.

        Args:
            exclude_impl : Iterable[ImplName]
                The names of the implementations to exclude from testing.
            stream: bool
                If set, will return a streamable version of the test function.

        Usage:
            ```python
            # All implementations except the given impl will be tested.

            @baml.OptionalFunc.test(exclude_impl=["implname"])
            async def test_logic(OptionalFuncImpl: IOptionalFunc) -> None:
                result = await OptionalFuncImpl(...)
            ```

            ```python
            # Streamable version of the test function.

            @baml.OptionalFunc.test(stream=True)
            async def test_logic(OptionalFuncImpl: IOptionalFuncStream) -> None:
                async for result in OptionalFuncImpl(...):
                    ...
            ```
        """
        ...

    @typing.overload
    def test(self, test_class: typing.Type[CLS]) -> typing.Type[CLS]:
        """
        Provides a pytest.mark.parametrize decorator to facilitate testing different implementations of
        the OptionalFuncInterface.

        Args:
            test_class : Type[CLS]
                The test class to be decorated.

        Usage:
        ```python
        # All implementations will be tested in every test method.

        @baml.OptionalFunc.test
        class TestClass:
            def test_a(self, OptionalFuncImpl: IOptionalFunc) -> None:
                ...
            def test_b(self, OptionalFuncImpl: IOptionalFunc) -> None:
                ...
        ```
        """
        ...

BAMLOptionalFunc: IBAMLOptionalFunc
