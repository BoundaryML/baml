# This file is generated by the BAML compiler.
# Do not edit this file directly.
# Instead, edit the BAML files and recompile.

# ruff: noqa: E501,F401
# flake8: noqa: E501,F401
# pylint: disable=unused-import,line-too-long
# fmt: off

from baml_core.stream import AsyncStream
from typing import Callable, Protocol, runtime_checkable


import typing

import pytest
from contextlib import contextmanager
from unittest import mock

ImplName = typing.Literal["default_config"]

T = typing.TypeVar("T", bound=typing.Callable[..., typing.Any])
CLS = typing.TypeVar("CLS", bound=type)


IHaikuAlphaOutput = str

@runtime_checkable
class IHaikuAlpha(Protocol):
    """
    This is the interface for a function.

    Args:
        product_review: str

    Returns:
        str
    """

    async def __call__(self, *, product_review: str) -> str:
        ...

   

@runtime_checkable
class IHaikuAlphaStream(Protocol):
    """
    This is the interface for a stream function.

    Args:
        product_review: str

    Returns:
        AsyncStream[str, str]
    """

    def __call__(self, *, product_review: str
) -> AsyncStream[str, str]:
        ...
class BAMLHaikuAlphaImpl:
    async def run(self, *, product_review: str) -> str:
        ...
    
    def stream(self, *, product_review: str
) -> AsyncStream[str, str]:
        ...

class IBAMLHaikuAlpha:
    def register_impl(
        self, name: ImplName
    ) -> typing.Callable[[IHaikuAlpha, IHaikuAlphaStream], None]:
        ...

    async def __call__(self, *, product_review: str) -> str:
        ...

    def stream(self, *, product_review: str
) -> AsyncStream[str, str]:
        ...

    def get_impl(self, name: ImplName) -> BAMLHaikuAlphaImpl:
        ...

    @contextmanager
    def mock(self) -> typing.Generator[mock.AsyncMock, None, None]:
        """
        Utility for mocking the HaikuAlphaInterface.

        Usage:
            ```python
            # All implementations are mocked.

            async def test_logic() -> None:
                with baml.HaikuAlpha.mock() as mocked:
                    mocked.return_value = ...
                    result = await HaikuAlphaImpl(...)
                    assert mocked.called
            ```
        """
        ...

    @typing.overload
    def test(self, test_function: T) -> T:
        """
        Provides a pytest.mark.parametrize decorator to facilitate testing different implementations of
        the HaikuAlphaInterface.

        Args:
            test_function : T
                The test function to be decorated.

        Usage:
            ```python
            # All implementations will be tested.

            @baml.HaikuAlpha.test
            async def test_logic(HaikuAlphaImpl: IHaikuAlpha) -> None:
                result = await HaikuAlphaImpl(...)
            ```
        """
        ...

    @typing.overload
    def test(self, *, exclude_impl: typing.Iterable[ImplName] = [], stream: bool = False) -> pytest.MarkDecorator:
        """
        Provides a pytest.mark.parametrize decorator to facilitate testing different implementations of
        the HaikuAlphaInterface.

        Args:
            exclude_impl : Iterable[ImplName]
                The names of the implementations to exclude from testing.
            stream: bool
                If set, will return a streamable version of the test function.

        Usage:
            ```python
            # All implementations except the given impl will be tested.

            @baml.HaikuAlpha.test(exclude_impl=["implname"])
            async def test_logic(HaikuAlphaImpl: IHaikuAlpha) -> None:
                result = await HaikuAlphaImpl(...)
            ```

            ```python
            # Streamable version of the test function.

            @baml.HaikuAlpha.test(stream=True)
            async def test_logic(HaikuAlphaImpl: IHaikuAlphaStream) -> None:
                async for result in HaikuAlphaImpl(...):
                    ...
            ```
        """
        ...

    @typing.overload
    def test(self, test_class: typing.Type[CLS]) -> typing.Type[CLS]:
        """
        Provides a pytest.mark.parametrize decorator to facilitate testing different implementations of
        the HaikuAlphaInterface.

        Args:
            test_class : Type[CLS]
                The test class to be decorated.

        Usage:
        ```python
        # All implementations will be tested in every test method.

        @baml.HaikuAlpha.test
        class TestClass:
            def test_a(self, HaikuAlphaImpl: IHaikuAlpha) -> None:
                ...
            def test_b(self, HaikuAlphaImpl: IHaikuAlpha) -> None:
                ...
        ```
        """
        ...

BAMLHaikuAlpha: IBAMLHaikuAlpha
