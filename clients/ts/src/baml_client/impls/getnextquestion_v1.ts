// This file is auto-generated. Do not edit this file manually.
//
// Disable formatting for this file to avoid linting errors.
// tslint:disable
// @ts-nocheck

import { Deserializer } from '../../baml_lib/deserializer/deserializer';
import { Main } from '../client';
import { GetNextQuestion } from '../function';
import { schema } from '../json_schema';
import { InternalMeetingRequestPartial } from '../types_internal';


const prompt_template = `\
Given the partial requirements, are the requirements complete?
Don't add any new fields to the output format and return the exact response.

Input:
\`\`\`
{//BAML_CLIENT_REPLACE_ME_MAGIC_input//}
\`\`\`

Output JSON:
{
  // True if there is enough information is filled out in the input. I don't need any more fields.
  "complete": bool,
  // If not complete, what question should be asked next? Try to gather as much information as possible in one question as this is answered in an email.
  "follow_up_question": string | null
}

JSON:\
`;

const deserializer = new Deserializer<Validation>(schema, {
  $ref: '#/definitions/GetNextQuestion_output'
});

GetNextQuestion.registerImpl('v1', {
  name: 'v1',
  run: async (
arg: MeetingRequestPartial
  ): Promise<Validation> => {
  
    const result = await Main.run_prompt_template(
      prompt_template,
      {
        "{//BAML_CLIENT_REPLACE_ME_MAGIC_input//}": arg,
      }
    );

    return deserializer.coerce(result);
  }
});
