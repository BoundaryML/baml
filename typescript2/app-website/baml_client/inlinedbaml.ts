/*************************************************************************************************

Welcome to Baml! To use this generated code, please run one of the following:

$ npm install @boundaryml/baml
$ yarn add @boundaryml/baml
$ pnpm add @boundaryml/baml

*************************************************************************************************/

// This file was generated by BAML: please do not edit it. Instead, edit the
// BAML files and re-generate this code using: baml-cli generate
// You can install baml-cli with:
//  $ npm install @boundaryml/baml
//
/* eslint-disable */
// tslint:disable
// @ts-nocheck
// biome-ignore format: autogenerated code

const fileMap = {
  
  "clients.baml": "// Learn more about clients at https://docs.boundaryml.com/docs/snippets/clients/overview\n\nclient<llm> CustomGPT4o {\n  provider openai\n  options {\n    model \"gpt-4o\"\n    api_key env.OPENAI_API_KEY\n  }\n}\n\nclient<llm> CustomGPT4oMini {\n  provider openai\n  retry_policy Exponential\n  options {\n    model \"gpt-4o-mini\"\n    api_key env.OPENAI_API_KEY\n  }\n}\n\nclient<llm> CustomSonnet {\n  provider anthropic\n  options {\n    model \"claude-3-5-sonnet-20241022\"\n    api_key env.ANTHROPIC_API_KEY\n  }\n}\n\n\nclient<llm> CustomHaiku {\n  provider anthropic\n  retry_policy Constant\n  options {\n    model \"claude-3-haiku-20240307\"\n    api_key env.ANTHROPIC_API_KEY\n  }\n}\n\n// https://docs.boundaryml.com/docs/snippets/clients/round-robin\nclient<llm> CustomFast {\n  provider round-robin\n  options {\n    // This will alternate between the two clients\n    strategy [CustomGPT4oMini, CustomHaiku]\n  }\n}\n\n// https://docs.boundaryml.com/docs/snippets/clients/fallback\nclient<llm> OpenaiFallback {\n  provider fallback\n  options {\n    // This will try the clients in order until one succeeds\n    strategy [CustomGPT4oMini, CustomGPT4oMini]\n  }\n}\n\n// https://docs.boundaryml.com/docs/snippets/clients/retry\nretry_policy Constant {\n  max_retries 3\n  // Strategy is optional\n  strategy {\n    type constant_delay\n    delay_ms 200\n  }\n}\n\nretry_policy Exponential {\n  max_retries 2\n  // Strategy is optional\n  strategy {\n    type exponential_backoff\n    delay_ms 300\n    mutliplier 1.5\n    max_delay_ms 10000\n  }\n}",
  "completion.baml": "\n\nfunction Completion(prefix: string, suffix: string, language: string) -> string {\n  client \"openai/gpt-4o\"\n  prompt ##\"\n    {{ _.role(\"system\", cache_control={\"type\": \"ephemeral\"}) }}\n\n    You are a programmer that suggests code completions in the %INSERT-HERE% part below with  {{ language }} code. Only output the code that replaces %INSERT-HERE% part, NOT THE SUFFIX OR PREFIX. Respond only with code, and with no markdown formatting.\n\n    Try to complete a whole section inside curlies when you can.\n\n    {% if language == \"baml\" %}\n    {{ BAMLBackground2()}}\n\n    Examples:\n    INPUT:\n    ---\n    class MyObject {{\"{\"}}%INSERT-HERE%\n    }\n    ---\n    OUTPUT:\n    ---\n      property string\n    ---\n    In this example, we just inserted one line, with tabs for a fake property to aid the user.\n\n    INPUT:\n    ---\n    function foo(input: string) -> string {{\"{\"}} %INSERT-HERE%\n      prompt #\"\n        {{ \"{{ input }}\" }}\n      \"#\n    }\n    ---\n    OUTPUT:\n    ---\n      client \"openai/gpt-4o\"\n    ---\n    In this example, no need to add the prompt because it was part of the suffix after INSERT-HERE\n\n    INPUT:\n    OUTPUT: N/A\n    In this example there was nothing to complete, so we returned N/A.\n\n    Ignore the \"---\" in your outputs.\n    {% endif %}\n\n\n    {{ _.role(\"user\") }}\n    INPUT:\n    ---\n    {{ prefix }}%INSERT-HERE%{{ suffix }}\n    ---\n  \"##\n}\n\nfunction CompletionPython(prefix: string, suffix: string, language: string) -> string {\n  client \"openai/gpt-4o\"\n  prompt ##\"\n    {{ _.role(\"system\", cache_control={\"type\": \"ephemeral\"}) }}\n\n    You are a programmer that suggests code completions in the %INSERT-HERE% part below with  {{ language }} code. Only output the code that replaces %INSERT-HERE% part, NOT THE SUFFIX OR PREFIX. Respond only with code, and with no markdown formatting.\n\n    Try to complete a whole section inside curlies when you can.\n\n\n\n    Examples:\n    INPUT:\n    ---\n    class MyClass:%INSERT-HERE%\n        pass\n    ---\n    OUTPUT:\n    ---\n        def __init__(self):\n            self.property = \"\"\n    ---\n    In this example, we just inserted one line, with spaces for a constructor with a property to aid the user.\n\n    INPUT:\n    ---\n    def process_text(text: str) -> str:%INSERT-HERE%\n        return text.upper()\n    ---\n    OUTPUT:\n    ---\n        \"\"\"Process the input text and return transformed version\"\"\"\n    ---\n    In this example, we added a docstring before the function body\n\n    INPUT:\n    OUTPUT: N/A\n    In this example there was nothing to complete, so we returned N/A.\n\n    Remove the \"---\" in your outputs.\n\n    {{ _.role(\"user\") }}\n    INPUT:\n    ---\n    {{ prefix }}%INSERT-HERE%{{ suffix }}\n    ---\n  \"##\n}\n\ntest TestName {\n  functions [CompletionPython]\n  args {\n    prefix #\"\n      hello world\n    \"#\n    suffix #\"\n      hello world\n    \"#\n    language #\"\n      hello world\n    \"#\n  }\n}\n\n\nfunction CompletionTypescript(prefix: string, suffix: string, language: string) -> string {\n  client \"openai/gpt-4o\"\n  prompt ##\"\n    {{ _.role(\"system\", cache_control={\"type\": \"ephemeral\"}) }}\n\n    You are a programmer that suggests code completions in the %INSERT-HERE% part below with  {{ language }} code. Only output the code that replaces %INSERT-HERE% part, NOT THE SUFFIX OR PREFIX. Respond only with code, and with no markdown formatting.\n\n    Try to complete a whole section inside curlies when you can.\n\n\n    Examples:\n    INPUT:\n    ---\n    class MyClass {{\"{\"}}%INSERT-HERE%\n      private name: string;\n    }\n    ---\n    OUTPUT:\n    ---\n      constructor() {\n        this.name = \"\";\n      }\n    ---\n    In this example, we just inserted a constructor with a property initialization to aid the user.\n\n    INPUT:\n    ---\n    function processText(text: string): string {{\"{\"}}%INSERT-HERE%\n      return text.toUpperCase();\n    }\n    ---\n    OUTPUT:\n    ---\n      // Process the input text and return transformed version\n    ---\n    In this example, we added a comment before the function body\n\n    INPUT:\n    OUTPUT: N/A\n    In this example there was nothing to complete, so we returned N/A.\n\n    Remove the \"---\" in your outputs.\n\n    {{ _.role(\"user\") }}\n    INPUT:\n    ---\n    {{ prefix }}%INSERT-HERE%{{ suffix }}\n    ---\n  \"##\n}\n\n\ntest CompletionTest {\n  functions [Completion]\n  args {\n    prefix \"function foo(input: string) -> string {\"\n    suffix \"\"\n    language \"baml\"\n  }\n}\n\ntest CompletionTest3 {\n  functions [Completion]\n  args {\n    prefix ##\"function foo(input: string) -> string {\n      client \"openai/gpt-4o\"\n      prompt #\"\n    \"##\n    suffix \"\"\n    language \"baml\"\n  }\n}\n\ntest CompletionTest2 {\n  functions [Completion]\n  args {\n    prefix \"function foo(input: string) -> string {\\n\"\n    suffix \"\\n  prompt #\\n\\\"\"\n    language \"baml\"\n  }\n}\n\n\ntemplate_string BAMLBackground2() ##\"\n  <Overview>\n    BAML is a domain-specific language for building LLM prompts as functions.\n    You can build an agentic workflow with BAML.\n  </Overview>\n\n  <Schema>\n    // Define output schemas using classes\n    class MyObject {\n      // Optional string fields use ?\n      // @description is optional, but if you include it, it goes after the field.\n      name string? @description(\"The name of the object\")\n\n      // Arrays of primitives\n      // arrays cannot be optional.\n      tags string[]\n\n      // Enums must be declared separately and are optional\n      status MyEnum?\n\n      // Union types\n      type \"success\" | \"error\"\n\n      // Primitive types\n      count int\n      enabled bool\n      score float\n\n      // nested objects\n      nested MyObject2\n\n      // image type\n      myImg image\n\n      // checks and assertions. Uses jinja syntax inside the parentheses.\n      // For a single property use one @\n      bar int @assert(between_0_and_10, {{ \"{{ this > 0 and this < 10 }}\" }}) //this = MyObject.bar value\n      quux string\n      // assertions for multiple fields use @@ and go at the bottom of the class. Uses jinja syntax inside the parentheses.\n      // Do NOT add descriptions after the assertion.\n      @@assert(length_limit, {{ \"{{ this.quux|length < this.baz }}\" }})\n    }\n\n    // Enums are declared separately\n    enum MyEnum {\n      PENDING\n      ACTIVE @description(\"Item is currently active\")\n      COMPLETE\n    }\n\n    // Comments use double slashes\n    // Recursive types and inline definitions are not supported\n  </Schema>\n\n  <Functions>\n    // Functions define inputs, outputs and prompts\n    // function name is always PascalCase\n    function MyFunction(input: MyObject) -> string {\n      client \"openai/gpt-4o\"\n      // prompt with jinja syntax inside here. with double curly braces for variables.\n      // make sure to include: {{ \"{{ ctx.output_format }}\"}} in the prompt, which prints the output schema instructions so the LLM returns the output in the correct format (json or string, etc.). DO NOT write the output schema manually.\n      prompt #\"\n\n      \"#\n    }\n\n    <LLMClients>\n      You can use any of the following:\n      - openai/gpt-4o\n      - openai/gpt-4o-mini\n      - anthropic/claude-3-5-sonnet-latest (note the \"3-5\")\n      - anthropic/claude-3-5-haiku-latest\n    </LLMClients>\n\n    <Prompt>\n      When writing the prompt:\n      1. Make sure to include the input in the prompt (even if it's an image) using {{ \"{{ input }}\" }}\n      2. Make sure to include {{ \"{{ ctx.output_format }}\" }} in the prompt so the LLM knows how to format the output.\n      3. You do not need to specify to \"answer in JSON format\". Only write in the prompt brief instruction, and any other task-specific things to keep in mind for the task.\n      4. Write a {{ \"{{ _.role(\\\"user\\\") }}\" }} tag to indicate where the user's inputs start. So if there's a convo you can write\n      #\"{{ \"{{ _.role(\\\"user\\\") }}\" }} {{ \"{{ some-variable }}\" }}#\n    </Prompt>\n  </Functions>\n\n  The @asserts only go in the \"output\" types. Don't use them in inputs.\n  Do NOT use numbers as confidence intervals if you need to use them. Prefer an enum with descriptions or literals like \"high\", \"medium\", \"low\".\n\n  Dedent all declarations.\n\"##\n\ntemplate_string BamlTests() ##\"\n  You will generate them in the BAML syntax, which follows the following format:\n  // Tests in BAML follow this format:\n  // test TestName {\n  //   functions [FunctionToTest]\n  //   args {\n  //     argumentName value\n  //   }\n  // }\n\n  // For primitive inputs (string, int, float, bool):\n  test SimpleTest {\n    functions [MyFunction]\n    args {\n      input {\n        name \"Test Object\"\n        tags [\"tag1\", \"tag2\"]\n        status ACTIVE\n        type \"success\"\n        count 42\n        enabled true\n        score 9.5\n      }\n    }\n  }\n\n  // For image inputs:\n  test ImageTest {\n    functions [MyFunction]\n    args {\n      imageArg {\n        file \"../images/test.png\"\n        // Optional: media_type \"image/png\"\n      }\n      // Or using URL:\n      // imageArg {\n      //   url \"https://example.com/image.png\"\n      // }\n    }\n  }\n\n  // For array/object inputs:\n  test ComplexTest {\n    functions [MyFunction]\n    args {\n      input {\n        name \"Complex Object\"\n        tags [\n          \"tag1\",\n          #\"\n            Multi-line\n            tag here\n          \"#\n        ]\n        status PENDING\n        type \"error\"\n        count 100\n        enabled false\n        score 7.8\n      }\n    }\n  }\n\"##\n",
  "convert-to-markdown.baml": "\n\nfunction ConvertToMarkdown(input: string) -> string {\n  client CustomGPT4oMini\n  prompt ##\"\n    Take the following input and convert it to a mdx markdown.\n\n    Don't include new lines between lists.\n    Use\n\n    Don't include start or end\n    ```mdx\n    ---\n    ---\n    ```\n\n    {{ _.role(\"user\") }}\n    INPUT:\n    ---\n    {{ input }}\n    ---\n \"##\n}\n\ntest ConvertToMarkdownTest {\n  functions [ConvertToMarkdown]\n  args {\n    input ##\"\n      Disclaimer, we build BAML (https://www.github.com/boundaryml/baml), a new syntax for building AI applications and agents.\n\n\\#\\# How Syntax Shapes Thinking\n\nThe concepts a syntax exposes fundamentally changes how developers think about problems and solve them. Let’s explore an example from web development.\n\nWe used to write websites like this:\n\n```python\ndef home():\n  return \"\"\"\n<button onClick=\"() => console.log('Button clicked!')\">Click me</button>\n\"\"\"\n```\n\nThis approach of mixing JavaScript, HTML, CSS, and backend logic proved unmaintainable. A single unclosed tag could break an entire site – an error that a compiler could catch in milliseconds, yet our tooling at that time didn't.\n\n\\#\\#\\# React Entered The Scene\n\nReact transformed web development by asking: \"What if we brought HTML and JavaScript into the same syntax?\" This led to JSX (and later TSX), which solved several critical problems:\n\n- Enforced component-based architecture\n- Standardized state management\n- Enabled compile-time error checking\n\nThis wasn't just a theoretical improvement. Facebook literally made React because their newsfeed had become too complex to maintain using traditional approaches.\n\nTailwind CSS followed a similar pattern by bringing CSS into React components. Traditional `style.css` files were full of custom 1-off classes that, in practice, were rarely reused. Turns out, the answer was always inline css, just with different syntax and amazing dev-x.\n\n```tsx\n// inline CSS\n<div style=\"{ 'background': 'red' }\"/>\n// Tailwind\n<div className=\"bg-red-300\" />\n```\n\nhttps://www.swyx.io/why-tailwind\n\n![Screenshot 2024-12-10 at 9.49.00 PM.png](https://prod-files-secure.s3.us-west-2.amazonaws.com/bfebc62c-55b5-46d2-9c18-8e5d5f1613dd/e0727a0c-bfb6-4179-b2db-1a3d465584e3/Screenshot_2024-12-10_at_9.49.00_PM.png)\n\n5 years ago, I felt the exact same way as @swyx about React. Being a C++ dev, I didn’t want to learn React. It felt like bloatware. So, I wrote my own React [https://github.com/vaibhav-trq/SecretSanta/blob/main/public/src/js/models/page.ts], router and statemanagement and all. I was wrong. React is better. Please forgive my sins.\n\n\\#\\# Engineering problems with Agents\n\n- **Prompts should be easy to find and read.** Fundamentally they are strings like HTML, but often they are incredibly dynamic - closer to TSX. Strings everywhere are horribly unmaintable.\n    - Why not TSX? Not everyone in the world uses Javascript/Typescript. But everyone will use AI.\n- **Swapping models should be easy.** Models get better every week, your app should too.\n- **You need a hot-reload loop.** When building a frontend, you change your code, cmd+s, the browser reloads (while persisting state from parent components)! Whats the equivalent for agents?\n- **It should work with any language (C++, Java, Ruby, …).** Every existing piece of software will eventually be agentic. We shouldn’t have to stand up a python microservice to build anything LLM related.\n- **You need structured outputs**. Not every model will support this, and traditional implementations degrade quality.\n- **Agents stream** - and streaming requires a different type-system. If the LLM returns a `T[]`, while streaming I likely want a `Partial<T>[]` . This is even more complicated for multi-step agents.\n- **No internet dependency** - I don’t want my app to require a paid SAAS to call a REST API.\n\n\\#\\# Our Proposal: Basic-Ass Machine Learning (BAML)\n\n*or “Basically a Made-up Language” for your boss*\n\nThis is our attempt at a syntax that starts to address some of these problems.\n\nIn some ways the problem of creating a new syntax is even harder than the ones listed above. Everything is fair game when making new syntax. If you can code it, it can be yours. We needed a design philosophy to help restrict ideas:\n\n\\#\\#\\# BAML’s Design Philosophy\n\n**1:** Avoid invention when possible\n\nYes, prompts need versioning — we have a great versioning tool: git\n\nYes, you need to save and iterate on prompts — we have a great storage: filesystems\n\n**2:** Any file editor and any terminal should be enough to use it\n\n**3:** Be fast\n\n**4:** A first year university student should be able to understand it\n\n\\#\\#\\# The core BAML principle: LLM Prompts are functions\n\nThe fundamental building block in BAML is a function. Every prompt is a function that takes in parameters and returns a type.\n\n```tsx\nfunction ChatAgent(message: Message[], tone: \"happy\" | \"sad\") -> string\n```\n\nEvery function additionally defines which models it uses and what its prompt is.\n\n```tsx\nfunction ChatAgent(message: Message[], tone: \"happy\" | \"sad\") -> string {\n  client \"openai/gpt-4o\"\n\n  prompt \\#\"\n    Be a {{ tone }} bot.\n\n    {% for m in message %}\n    {{ _.role(m.role) }}\n    {{ m.content }}\n    {% endfor %}\n  \"\\#\n\n}\n\nclass Message {\n  role string\n  content string\n}\n```\n\n- Introducing Strings 2.0\n\n    The best prompting framework is one that gives us the flexibility of english, but with the structure of code.\n\n    - Strings are automatically dedented and trimmed. Great for readability, and exactly what you want in your prompts.\n    - Since our strings start with `\\#\"` and end with `\"\\#`  you need virtually no escape characters.\n    - Yes, you can using `\"\\#` in your strings. Just use `\\#\\#\\#\"` `\"\\#\\#\\#` to start and stop your strings.\n- Making Jinja better\n    - The FIRST and only static analyzer for Jinja, AKA: prompt strings (typescript level)\n    - We disabled macros — instead type-safe `template_string`\n\n        ```python\n        template_string MyReusableString(topic: string) \\#\"\n          Talk about {{ topic }}.\n        \"\\#\n\n        function ChatAgent(...) -> string {\n          client \"openai/gpt-4o\"\n        \tprompt \\#\"\n        \t  {{ MyReusableString(\"horses\") }}\n        \t\"\\#\n        }\n```\n\n\n\\#\\#\\# Making prompts easy to find and read\n\nSince every prompt is a function, you can use standard LSP (Language Server Protocols) tools to find every prompt you’ve written. But we’ve taken BAML one step further and built native tooling for VSCode ( jetbrains + neovim coming soon).\n\n1. You can see the full prompt (including any multi-modal assets).\n\n<gif of prompt preview with an image. Open .baml file (with image). click open playground. scroll through prompt (inlcude 2 paramaters so the input string also shows highlighted)>\n\n2. You can see the exact network request we are making\n\n<gif of swapping to curl raw curl (in baml playgroaund already open, switch to raw curl)>\n\n3. You can see every function you’ve ever written\n\n<image of dropdown showing every function>\n\n\n\\#\\#\\# **Swapping models: 1-line changes**\n\nIts just 1 line.\n\n<image of git diff going from o1 → anthropic sonnet + some funny comment, use shorthand syntax>\n\nRetries and fallbacks? All statically defined.\n\n<image left: code showing retry + fallback defintions, right: client graph. maybe gif of retry count changing, and client graph changing>\n\nWant to do this dynamically at runtime? <link to Client Registry>\n\n\\#\\#\\# **Hot-reloading for prompts**\n\nUsing AI is all about iteration speed.\n\nIf testing your pipeline takes 2 minutes, in 20 minutes, you can only test 10 ideas.\n\nIf testing your pipeline took 5 seconds, in 20 minutes, you can test 240 ideas.\n\nIntroducing testing, for prompts.\n\n<gif of code on the left, playground already open on the right. You hit “run test” above the test case>\n\n\\#\\#\\# Plugging into every programming language\n\n< architecture diagram , BAML file → baml client in the language of your choice → how baml parses the result for you>\n\nSince BAML is a configuration file format, the BAML toolchain allows you to transpile BAML into native Python, Typescript, Ruby, etc code. So you get autocomplete and static analysis without thinking about it. Plus, our compiler is fast - it takes less than 30 ms to generate code!\n\n<gif of autocomplete / errors in python + typescript next to a BAML function definition>\n\nAll interfaces use the same Rust core, so no more “typescript is broken!”. Every language we natively support is Tier 1 and “works”. Every feature ships to all of them at the same time.\n\nFor languages we don’t yet have native support for, BAML can also turn into a local REST API Server and use OpenAPI to generate a typesafe client. Devs are using this for C++, Java, Erlang, Go, PHP etc). However, we do plan on adding more languages with native codegen! Just takes time. (PRs Welcome! 🙃)\n\n\\#\\#\\# **Structured outputs - but better**\n\nJSON is amazing for REST APIs, but way too strict and verbose for LLMs. LLMs need something flexible. We created the SAP (schema-aligned parsing) algorithm to support the flexible outputs LLMs can provide, like markdown within a json blob or chain-of-thought prior to answering.\n\n<gif of some schema from an LLM with chain of thought + no quotes + triple quote strings for multiline markdown>\n\nSAP works with any model on day-1, without depending on tool-use or function-calling APIs. And it all runs without any additional\n\n<link to R1 blog>\n\n<link to O1 blog>\n\n\\#\\#\\# Streaming (when it’s a first class citizen)\n\nStreaming is way harder than it should be. With our [python/typescript/ruby] generated code, here’s what it looks like\n\n<gif coding b.stream.*, show the type during streaming is different than when done>\n\n\\#\\#\\# No strings attached\n\n- 100% open-source (Apache 2)\n- 100% private. AGI will not require an internet connection, neither will BAML\n- No network requests beyond the model call you explicitly set\n- Not stored or used for any training data\n- BAML files can be saved locally on your machine and checked into Github for easy diffs.\n\n\\#\\# Conclusion\n\nAs models get better, we’ll continue expecting even more out of them. But what will never change is that we’ll want a way to write maintainable code that uses those models. The current way we all just assemble strings is very reminiscent of the early days PHP/HTML soup in web development. We hope some of the ideas we shared today can make a tiny dent in helping us all shape the way we all code tomorrow.\n\n\\#\\# Trying BAML\n\n- Online - no installs - [promptfiddle.com](http://promptfiddle.com)\n- Learn BAML fast - boundaryml.com/baml\n- Locally - follow our guide based on your setup preference\n- Link to docs\n\n\"##\n}\n}\n",
  "generators.baml": "// This helps use auto generate libraries you can use in the language of\n// your choice. You can have multiple generators if you use multiple languages.\n// Just ensure that the output_dir is different for each generator.\ngenerator target {\n    // Valid values: \"python/pydantic\", \"typescript\", \"ruby/sorbet\", \"rest/openapi\"\n    output_type \"typescript\"\n\n    // Where the generated code will be saved (relative to baml_src/)\n    output_dir \"../\"\n\n    // The version of the BAML package you have installed (e.g. same version as your baml-py or @boundaryml/baml).\n    // The BAML VSCode extension version should also match this version.\n    version \"0.202.1\"\n\n    // Valid values: \"sync\", \"async\"\n    // This controls what `b.FunctionName()` will be (sync or async).\n    default_client_mode async\n}\n",
  "rag.baml": "class Citation {\n  number int @description(#\"\n    the index in this array\n  \"#)\n  documentTitle string\n  sourceLink string\n  relevantTextFromDocument string @alias(\"relevantSentenceFromDocument\") @description(#\"\n    The relevant text from the document that supports the answer. This is a citation. You must quote it EXACTLY as it appears in the document with any special characters it contains. The text should be contiguous and not broken up. You may NOT summarize or skip sentences. If you need to skip a sentence, start a new citation instead.\n  \"#)\n}\n\nclass Answer {\n  answersInText Citation[] @alias(\"relevantSentencesFromText\")\n  answer string @description(#\"\n    An answer to the user's question that MUST cite sources from the relevantSentencesFromText. Like [0]. If multiple citations are needed, write them like [0][1][2].\n  \"#)\n}\n\n\nclass Document {\n  title string\n  text string\n  link string\n}\nclass Context {\n  documents Document[]\n}\n\nfunction AnswerQuestion(question: string, context: Context) -> Answer {\n  client CustomGPT4oMini\n  prompt #\"\n    Answer the following question using the given context below.\n    CONTEXT:\n    {% for document in context.documents %}\n    ----\n    DOCUMENT TITLE: {{  document.title }}\n    {{ document.text }}\n    DOCUMENT LINK: {{ document.link }}\n    ----\n    {% endfor %}\n\n    {{ ctx.output_format }}\n\n    {{ _.role(\"user\") }}\n    QUESTION: {{ question }}\n\n    ANSWER:\n  \"#\n}\n\n// open this in the playground to run it instantly\ntest SampleTest {\n  functions [AnswerQuestion]\n  args {\n    question #\"\n      what achievements did spacex accomplish before anyone else?\n    \"#\n    context {\n      documents [\n        {\n    title \"SpaceX Overview\"\n    link \"https://en.wikipedia.org/wiki/SpaceX\"\n    text #\"\n    Space Exploration Technologies Corporation, commonly referred to as SpaceX, is an American spacecraft manufacturer, launch service provider and satellite communications company headquartered in Hawthorne, California. The company was founded in 2002 by Elon Musk with the goal of reducing space transportation costs and ultimately developing a sustainable colony on Mars. The company currently produces and operates the Falcon 9 and Falcon Heavy rockets along with the Dragon and Starship spacecraft.\n\nThe company offers internet service via its Starlink subsidiary, which became the largest-ever satellite constellation in January 2020 and, as of April 2024, comprised more than 6,000 small satellites in orbit.[8]\n\nMeanwhile, the company is developing Starship, a human-rated, fully-reusable, super heavy-lift launch system for interplanetary and orbital spaceflight. On its first flight in April 2023, it became the largest and most powerful rocket ever flown. The rocket reached space on its second flight that took place in November 2023.\n\nSpaceX is the first private company to develop a liquid-propellant rocket that has reached orbit; to launch, orbit, and recover a spacecraft; to send a spacecraft to the International Space Station; and to send astronauts to the International Space Station. It is also the first organization of any type to achieve a vertical propulsive landing of an orbital rocket booster and the first to reuse such a booster. The company's Falcon 9 rockets have landed and flown again more than 300 times.[9] As of December 2023, SpaceX has around US$180 billion valuation.[10][11]\nHistory\nMain article: History of SpaceX\nSee also: List of Falcon 9 and Falcon Heavy launches\n\n    \"#\n  }\n\n      ]\n    }\n  }\n}\n\n",
  "resume.baml": "// Defining a data model.\nclass Resume {\n  name string\n  email string\n  experience string[]\n  skills string[]\n}\n\n// Create a function to extract the resume from a string.\nfunction ExtractResume(resume: string) -> Resume {\n  // Specify a client as provider/model-name\n  // you can use custom LLM params with a custom client name from clients.baml like \"client CustomHaiku\"\n  client \"openai/gpt-4o\" // Set OPENAI_API_KEY to use this client.\n  prompt #\"\n    Extract from this content:\n    {{ resume }}\n\n    {{ ctx.output_format }}\n  \"#\n}\n\n// Test the function with a sample resume. Open the VSCode playground to run this.\ntest vaibhav_resume {\n  functions [ExtractResume]\n  args {\n    resume #\"\n      Vaibhav Gupta\n      vbv@boundaryml.com\n\n      Experience:\n      - Founder at BoundaryML\n      - CV Engineer at Google\n      - CV Engineer at Microsoft\n\n      Skills:\n      - Rust\n      - C++\n    \"#\n  }\n}\n",
}
export const getBamlFiles = () => {
    return fileMap;
}