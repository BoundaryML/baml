{
  "installCommand": "cd .. && corepack enable && corepack prepare pnpm@9.12.0 --activate && pnpm install --frozen-lockfile --prod=false",
  "buildCommand": "bash scripts/vercel-build.sh",
  "outputDirectory": ".next"
}
