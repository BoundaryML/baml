NEXT_PUBLIC_APP_ENV=development
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=/relay-JkOu

# Changelog microservice base URL (server-only; set in the Vercel project env).
CHANGELOG_API=

# Agent-tries-baml dashboard: the api service (Convex gateway) base URL + token (server-only).
SERVICE_URL=
SERVICE_TOKEN=
