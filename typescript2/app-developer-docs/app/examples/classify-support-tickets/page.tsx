import { AuthoredPage, authoredMetadata } from '@/components/authored-page';
export const metadata = authoredMetadata('/examples/classify-support-tickets');
export default function ClassifySupportTicketsExamplePage() {
  return <AuthoredPage path="/examples/classify-support-tickets" />;
}
