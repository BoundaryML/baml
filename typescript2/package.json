{
  "name": "baml-playground-v2",
  "version": "0.0.1",
  "private": true,
  "description": "BAML Playground v2 - VSCode extension and web app",
  "scripts": {
    "build:wasm": "wasm-pack build ../baml_language/crates/bridge_wasm --target web --out-dir ../../../typescript2/pkg-playground/wasm --release --scope b",
    "dev:wasm": "BRIDGE_WASM_FORCE_RERUN=1 npx nodemon --watch ../baml_language --ext rs,toml --exec \"pnpm build:wasm\"",
    "dev:webview": "pnpm --filter app-vscode-webview dev",
    "dev:cargo": "cargo watch -C ../baml_language --skip-local-deps --watch-when-idle -w crates -x 'build -p baml_cli'",
    "dev:vscode": "turbo run dev --filter=app-vscode-ext --filter=app-vscode-webview --filter=@b/pkg-playground '//#dev:cargo'",
    "dev:promptfiddle": "turbo run dev --filter app-promptfiddle --filter @b/pkg-playground '//#dev:wasm'",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "pnpm -r clean"
  },
  "packageManager": "pnpm@9.12.0",
  "devDependencies": {
    "turbo": "^2.8.9",
    "typescript": "^5.7.0"
  }
}
