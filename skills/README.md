# BAML agent skills

These skills are compiled into `baml-cli`. Update them with the toolchain code
that introduces or removes the behavior they describe.

The separate `BoundaryML/baml-skill` repository is legacy-only. Keep it
available for toolchains released before skills were embedded.
