{
  "name": "python-demo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run python hello.py",
    "build:baml": "bash ./build-baml.sh",
    "generate": "uv run baml-cli generate",
    "baml-cli": "uv run baml-cli",
    "sync": "uv sync",
    "setup": "pnpm sync && pnpm build:baml && pnpm generate",
    "test": "uv run pytest",
    "typecheck": "uv run mypy ."
  },
  "devDependencies": {}
}