function ExampleHeaders(value: int) -> int {
    //# setup
    let x = value;

    //# branching
    if (x > 0) {
        //## positive path
        1
    } else {
        //## non-positive path
        0
    }
}
