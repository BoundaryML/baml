import dotenv from 'dotenv';
let env = dotenv.config();

import * as f from "./function"
import "./impls"

const BamlClient = {
  {{#each functions as |f|}}
  {{f}}: f.{{f}},
  {{/each}}
}

export default BamlClient