{%- for enum in enums %}
export enum {{enum.name}} {
  {%- for value in enum.values %}
  {{ value }} = "{{ value }}",
  {%- endfor %}
}
{% endfor %}

{%- for cls in classes %}
export interface {{cls.name}} {
  {%- for (name, type) in cls.fields %}
  {{name}}: {{type}}
  {%- endfor %}
}
{% endfor %}