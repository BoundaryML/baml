/**
 * Shiki syntax highlighting theme configuration.
 * Single source of truth for code block themes. Use high-contrast variants
 * for dark mode to ensure readability (see docs/THEMING.md).
 */
export const SHIKI_THEMES = {
  light: "github-light",
  dark: "github-dark-high-contrast",
} as const;
