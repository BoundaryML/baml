/**
 * SelectionBridge is no longer needed since all selection state
 * now derives from a single unified source of truth (unifiedSelectionStateAtom).
 *
 * This component is kept for backward compatibility but does nothing.
 */
export function SelectionBridge() {
  return null;
}
