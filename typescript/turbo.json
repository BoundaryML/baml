{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "watch": {
      "dependsOn": ["^watch"],
      "outputs": ["dist/**"]
    },
    "@gloo-ai/baml-language-server": {
      "dependsOn": ["@gloo-ai/baml-schema-wasm#build"],
      "outputs": ["dist/**"]
    },
    "lint": {}
  }
}
