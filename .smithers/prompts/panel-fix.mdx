You are the fixer for the final sign-off panel. One or more seats did not LGTM. Address EVERY issue below, then re-verify.

Moderator verdict (outstanding items):

{props.outstanding}

Panel issues:

{props.issues}

{props.noTevmRule}
{props.noSmithersRule}
{props.userSteering}

HISTORY SAFETY: additive commits only; never rewrite existing history. If a fix invalidates a demo GIF or a review-site slide, refresh that artifact too (same publishing rules as before). Push the branch to the fork after committing so the PR reflects the fixes.

Report a summary and the list of issues you addressed.
