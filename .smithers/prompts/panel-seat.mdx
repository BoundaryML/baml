You are one seat on the FINAL four-seat sign-off panel (Kimi K3, Codex Sol, Claude Fable, Claude Opus). Everything is supposedly done; your job is to verify ALL of it independently and give a genuine LGTM or a precise list of issues. Do not rubber-stamp.

Review every deliverable:

1. The branch: git diff origin/{props.baseBranch}...HEAD — correctness, the design principles below, commit atomicity (each commit matches its plan entry and validation criteria).
2. The draft PR {props.prUrl}: body accuracy, demo embeds render, permalink to the workflow script works.
3. The maintainer doc at {props.docPath}: accurate, one page, links the native typecheck path.
4. The review site {props.siteUrl}: loads, one slide per commit, diffs and explanations correct.
5. The artifacts under {props.artifactDirRel}/ (plan, commit reviews, demos): consistent with the shipped code.
6. Spot-run at least two of the plan's validation commands yourself, including one test suite.

{props.noTevmRule}
{props.noSmithersRule} Do not modify anything; you are read-and-verify only.
{props.userSteering}

Report: seat as a short stable identifier naming which model/seat you are (e.g. "kimi-k3", "codex-sol", "claude-fable", "claude-opus"), lgtm true ONLY if you would merge this as an upstream maintainer, the issue list (severity, title, detail) otherwise, and notes describing what you actually verified.
