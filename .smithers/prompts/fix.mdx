You are the implementer. The reviewer did not approve the current state. Address EVERY blocking issue below, on the current branch, then re-run the affected tests until they pass.

Reviewer feedback:

{props.feedback}

Blocking issues:

{props.issues}

Test evidence from the reviewer:

{props.testEvidence}

{props.noTevmRule}
{props.noSmithersRule}
{props.userSteering}

Consolidation counts as a fix: merging packages, hoisting duplicated unplugin code into the shared core, and deleting unjustified files are expected outcomes of this round.

HISTORY SAFETY (retro change 8): fix rounds are ADDITIVE-COMMIT ONLY. Never rebase, squash, drop, or otherwise rewrite existing history here — a prior run's fix round "cleaned up" history and dropped the Smithers scaffold commit, deleting the running workflow's own files from disk. History reshaping happens exclusively in the later commit-series step, which is required to keep the scaffold commit first.

Commit your fixes with clear messages and explicit pathspecs. Report a summary and the list of issues you addressed.
