You are the sole implementer. Execute the final plan below END TO END in this BAML repository, on the current git branch (never switch branches; you are on the feature branch already).

=== FINAL PLAN ({props.planPath}) ===
{props.planArtifact}
=== END FINAL PLAN ===

{props.noTevmRule}
{props.noSmithersRule}
{props.userSteering}

Deliverables (all of them, no placeholders or stubs left behind):

1. The Rust/core technology package(s) exactly as the plan specifies (compiler bridge included).
2. The minimal unplugin bridge.
3. The complete TS language-service integration: virtual modules, generated types, source maps, and every editor feature working, explicitly including go-to-definition from TS usage into .baml declarations.
4. Integration and e2e tests proving each feature, wired into this repo's existing test conventions.
5. A DRAFT of the one-page maintainer architecture doc at the repo path the plan chose (a reviewer will finalize it later); report that path as the doc draft path.
6. A CLEAN-CONSUMER packed e2e (principle 4): npm pack every published package into a fresh scratch project and prove the full feature surface works with ZERO internal env vars or test-only hooks. This is the test that catches "works only on this machine / only under the test harness" defects.
7. DECLARATION/RUNTIME PARITY tests (principle 5): assert the runtime module exports exactly what the generated declarations claim, for every projected construct (functions, classes, enums, consts).

Working agreement:

- Commit your work incrementally with clear, conventional messages using explicit pathspecs (`git add <paths> && git commit`); never use git add -A.
- Run the relevant test suites as you go (cargo test --lib in baml_language/ for Rust changes, plus the TS package tests you add) and make them pass.
- Respect the repo's formatting rules (cargo fmt with the CONTRIBUTING.md config for Rust).
- Acceptance criteria: every deliverable above exists, the new tests pass locally, and pre-existing tests in the areas you touched still pass.

Report: status "complete" only when every deliverable is done and tests pass ("blocked" with a blockedReason otherwise), a thorough summary, the changed file paths, the exact test commands a reviewer should run, and the doc draft path.
