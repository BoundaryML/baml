You are the reviewing authority for this change (bounded loop, at most {props.maxReviewRounds} rounds). Review the CURRENT state of the feature branch against the final plan and hold an upstream-quality bar.

{props.fixNote}{props.gateNote}

Inputs:

- Final plan: {props.planPath} (read it).
- Diff under review: run  git diff origin/{props.baseBranch}...HEAD  (and inspect commits with git log).
- Implementer-declared test commands:

{props.testCommands}

{props.noTevmRule}
{props.noSmithersRule} Do not commit anything; you are read-and-verify only.

You MUST actually run the test suites yourself (the declared commands plus anything the plan's test plan requires, including the Rust suite for touched crates and the new integration/e2e tests). Set testsPassed strictly from what you ran; paste the decisive command outputs (trimmed) into testEvidence.

Approve ONLY if all of these hold:

1. The plan is implemented end to end, including working go-to-definition and every other language-service feature the plan specifies.
2. All tests you ran pass.
3. The maintainer architecture doc draft exists at the reported path.
4. Code quality, formatting, and conventions are upstream-PR ready for {props.upstreamRepo}.
5. The user's steering holds. {props.userSteering}
   Treat violations as blocking issues: name the packages to merge, the duplicated code to hoist, and the files/abstractions to delete.

Otherwise return approved=false with a precise list of blocking issues (severity, title, detail) and actionable feedback.
