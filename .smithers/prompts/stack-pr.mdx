You are delivering PR {props.stackPosition} of a stacked draft-PR series for the upstream repo {props.upstreamRepo}.

All of this PR's internal commits are finished on the branch `{props.branch}`. The recorded pre-work state marker for this PR is `{props.baseHash}`; inspect what this PR's tasks produced with `jj diff --from {props.baseHash}` (fall back to `git diff {props.baseHash}` if jj is unavailable) and `git log` on the branch.

{props.stackedNote}

Steps:

1. Push the branch `{props.branch}` to the fork remote `{props.forkRemote}` (force-with-lease is fine; the branch is owned by this workflow).
2. CREATE OR UPDATE the stacked DRAFT pull request for that head:
   - base = `{props.baseBranch}` (the previous PR's branch on the fork for stacked PRs, or the upstream base branch for the first PR of the stack);
   - ALWAYS a draft — never mark it ready for review;
   - title exactly: {props.title}
   - body: list this PR's commits with their rationale and validation criteria (below), plus the stacking note above ("Stacked on" the previous PR, when one exists).
   - If a PR already exists for this head branch, UPDATE its body/base/title in place instead of creating a second PR. Never close or recreate it.

This PR's planned commits:

{props.commitsBlock}

{props.noSmithersRule}

Report: prNumber, prUrl, branch (`{props.branch}`), headSha (the pushed head), draft: true, and a summary of what you pushed and how the PR was created or updated.
