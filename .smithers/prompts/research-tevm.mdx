You are a senior compiler-tooling engineer doing deep architecture research.

Goal: produce a SELF-CONTAINED architecture artifact describing how the TEVM monorepo at {props.tevmRepo} implements its TypeScript tooling stack. Later engineers in this pipeline are forbidden from reading TEVM at all; your artifact is their only window into it, so it must stand alone.

Read {props.tevmRepo} (read-only; change nothing there and nothing in this repo except the artifact file below).

Write the artifact as markdown to {props.artifactDirRel}/tevm-architecture.md (repo-relative; the directory already exists). Keep it under roughly 1500 lines while staying concrete: real package names, file paths, key APIs and types, data flow, and gotchas.

It MUST cover, each as its own section:

1. Core compiler/resolver pipeline (how .sol sources are located, compiled, cached).
2. The unplugin package: how one plugin core serves Vite/Webpack/Rollup/esbuild/Bun, and what the bridge actually does at build time.
3. The TypeScript language-service plugin: virtual modules and generated .d.ts types, how imports of non-TS sources typecheck.
4. Source maps: how generated TS/type positions map back to original source positions.
5. Each editor feature end to end: go-to-definition, find-references, rename, diagnostics, completions, hover.
6. Caching and HMR/watch behavior.
7. Packaging and distribution: package layout, exports maps, versioning, how the pieces depend on each other.
8. Testing strategy: unit, integration, e2e, fixtures; what gets tested where.

Close with a "transferable blueprint" section: the ordered steps another language would follow to replicate this stack.

CRITICAL FRAMING (retro change 1): capture TEVM's TECHNIQUES, never its repository structure as a prescription. Open the artifact with a prominent warning that TEVM's package split is an artifact of TEVM's monorepo conventions and MUST NOT be replicated — the consuming repo's own conventions govern layout, and downstream designers should default to the fewest packages that repo's conventions allow.

Report the repo-relative artifact path, a summary of at least a solid paragraph, and the list of section titles you covered.
