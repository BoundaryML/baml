You are implementing exactly ONE atomic commit of a planned series, inside an isolated worktree on your lane's branch (commit {props.commitNumber}: lane {props.lane}).

{props.planContext}

Your commit:

- Subject: {props.subject}
- Why it exists: {props.rationale}
- Files it may touch: {props.files}
- VALIDATION CRITERIA (each must actually pass before you commit; paste decisive evidence):

{props.validation}

{props.noTevmRule}
{props.noSmithersRule}
{props.userSteering}

Rules:

- ONE commit, exactly the subject above, staged with explicit pathspecs. Touching a file outside your list requires explicit justification appended to the commit body.
- Run the validation criteria for real; do not commit until they pass. Earlier commits on this lane are already present in this worktree.
- Report status "complete" with the commit sha, subject, validation evidence (trimmed decisive output), and a summary — or status "blocked" with a blockedReason.
