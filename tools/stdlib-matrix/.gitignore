# Generated intermediates: extractor/exporter outputs, regenerated by `run`
# from lockfile-pinned inputs (ts_surface.json) and the compiler at HEAD
# (baml_surface.json). Only the ratchet baseline under report/ is tracked.
data/

# Ratchet-run outputs: fresh reports are written as *.new.* and compared
# against the committed baseline; only the baseline is tracked.
report/*.new.*
