# Native macOS hello-world longevity harness

This harness compares Node and Python baselines, their BAML bridge equivalents, and a packed BAML executable without Docker. Every process is a native macOS arm64 release artifact and every target receives an independent Vegeta attack over loopback. The default run sends 300 requests/second to each target for five minutes, or 1,500 RPS total.

Each `GET /` returns exactly `hello world` with HTTP 200, `Content-Type: text/plain; charset=utf-8`, and `Cache-Control: no-store`. The two bridge variants call the deterministic BAML function through their process-wide runtimes on every request. The packed BAML target exercises the same function and BAML HTTP server without a language bridge.

## Build

Prerequisites are macOS on Apple Silicon, Rust and the repository's mise toolchain, Node 20, `uv`, npm, and network access for pinned dependencies and Vegeta 12.13.0. `build.py` compiles the CLI, pack host, Python bridge, and Node bridge from one selected `baml_language` workspace with the same `BAML_GIT_SHA`. It records the source revision, dirty state, platform, runtimes, and artifact hashes in `.build/manifest.json`.

```sh
cd tools/bench/hello-world-local-native
python3 scripts/build.py --baml-source /path/to/baml-worktree/baml_language
```

The default source is this repository's `baml_language` directory. Pass another worktree to compare a candidate fix. Generated SDKs, native addons, wheels, virtual environments, dependencies, and load tools remain under ignored `.build/`. A cold release build takes several minutes; after a completed build from the same exact source snapshot, `--skip-rust` reuses the matching native artifacts and refreshes the generated apps. The snapshot fingerprint includes tracked changes and nonignored untracked files, so two dirty states at the same commit cannot silently share artifacts.

The current Node bridge wrapper has pre-existing `HandleKey` versus protobuf `Long` declaration errors. The build follows the release experiment workflow and transpiles that wrapper with `tsc --noCheck`; it type-checks the generated demo SDK normally and exercises the resulting native addon during the run.

## Run

```sh
python3 scripts/run.py --rate 300 --duration 300
python3 scripts/run.py --rate 100 --duration 900
python3 scripts/run.py --rate 300 --duration 0  # continue until Ctrl-C
python3 scripts/run_cycled.py --rate 100 --duration 300 --on-seconds 30 --off-seconds 1
```

The runners own every child process, verify each exact response before load, probe each target during sampling, capture Vegeta HTTP status counters, sample native RSS and CPU with `ps`, and shut down only the processes they started. `run_cycled.py` keeps the applications alive while repeatedly starting load for the requested on period and leaving them idle for the requested off period. Results go to `results/<timestamp>-<revision>-<rate>rps/`, including the build manifest, configuration, process logs, samples, and a machine-readable summary with delivered RPS and an RSS slope fitted after the first minute.

| Variant | URL | Load metrics |
| --- | --- | --- |
| Node 20 / Express | http://127.0.0.1:8501/ | http://127.0.0.1:8611/metrics |
| Node 20 / Express + BAML | http://127.0.0.1:8502/ | http://127.0.0.1:8612/metrics |
| Python 3.10 / Starlette | http://127.0.0.1:8503/ | http://127.0.0.1:8613/metrics |
| Python 3.10 / Starlette + BAML | http://127.0.0.1:8504/ | http://127.0.0.1:8614/metrics |
| Packed BAML | http://127.0.0.1:8505/ | http://127.0.0.1:8615/metrics |

The harness does not impose a per-process memory limit on macOS, so it measures sustained RSS growth rather than an artificial 1 GiB OOM boundary. Vegeta validates HTTP statuses for every completed request; the runner checks the exact body and headers before load and on each sampling interval. Stop unrelated heavy workloads before comparing slopes. In particular, stop only the Compose load-generator services if the Docker longevity harness is still running so its apps, Grafana, Prometheus, and historical data remain available.

## Initial native reproduction

The first 15-minute run used the combined PR #4847 + PR #4811 worktree at `42365a45f9d7a3750ed72bd338006d369f6c0e41`, macOS arm64, Node 20.14.0, Python 3.10.0, and 300 RPS per target. All five targets stayed alive and returned only HTTP 200 responses, with approximately 272,400 completed requests each. After excluding the first minute, Node bridge RSS grew 16.46 MiB/min and Python bridge RSS grew 16.45 MiB/min. Python baseline was flat, Node baseline grew 0.31 MiB/min, and packed BAML grew 1.27 MiB/min. This reproduces the bridge-specific memory growth without Docker; [the retained summary](evidence/2026-09-11-pr4847-4811-300rps/summary.json) contains the measured values.

## Quiet-window control without the background-GC fix

A paired five-minute experiment rebuilt the native artifacts once from the same clean pre-#4870 commit `42365a45f9d7a3750ed72bd338006d369f6c0e41`, then ran 100 RPS per target continuously and with 30 seconds on followed by one second off. The cycled run started 10 load periods, accumulated 290.55 loaded seconds, delivered about 29,000 HTTP 200 responses per target at 100 RPS, and kept every target healthy. The continuous control delivered 30,000 HTTP 200 responses per target at 100 RPS and also kept every target healthy.

The comparison uses the change between median RSS in minutes 2–3 and minutes 4–5, divided by two minutes, so one abrupt Node allocator release near the end of the continuous run does not dominate the result. Node BAML grew 4.94 MiB/min continuously and 1.03 MiB/min with quiet windows; Python BAML grew 5.32 MiB/min continuously and 0.14 MiB/min with quiet windows. Over the same windows, the Node baselines measured 1.14 and -0.50 MiB/min respectively, while both Python baselines were within 0.03 MiB/min of flat. In the cycled run, Node BAML moved from 163.69 MiB at two minutes to 166.64 MiB at the last sample, and Python BAML moved from 78.95 MiB to 79.23 MiB.

This is evidence that regular one-second gaps let the existing 100 ms idle collector prevent the persistent bridge growth seen under continuous load. It rules out the original roughly request-proportional leak over this five-minute profile, but it does not prove that a slower leak cannot appear in a longer run. The retained [paired comparison](evidence/2026-09-14-pre4870-cycled-vs-continuous-100rps/comparison.json), [cycled summary](evidence/2026-09-14-pre4870-cycled-vs-continuous-100rps/cycled-summary.json), and [continuous summary](evidence/2026-09-14-pre4870-cycled-vs-continuous-100rps/continuous-summary.json) record the derived measurements and request validation; both raw sample streams and build manifests are retained alongside them.

## Pure BAML explicit-GC checkpoints

The pure-BAML diagnostic requires `baml.sys.heap_stats()`. Build it explicitly from a source checkout containing that read-only diagnostic and run 30-second load periods separated by forced major collections:

```sh
python3 scripts/build.py --baml-source /path/to/heap-stats-worktree/baml_language --explicit-gc-diagnostic
python3 scripts/run_explicit_gc.py --rate 100 --duration 300 --load-seconds 30 --post-gc-seconds 2 --interval 5
python3 scripts/run_explicit_gc.py --target python-baml --rate 100 --duration 300 --load-seconds 30 --post-gc-seconds 2 --interval 5
python3 scripts/run_explicit_gc.py --target python-baml --workload response-allocation --rate 2000 --duration 60 --load-seconds 60 --post-gc-seconds 1 --interval 1
python3 scripts/run_allocation_attribution.py
python3 scripts/run_allocation_attribution.py --no-malloc-stack-logging
python3 scripts/build_malloc_pressure_relief.py --baml-source /path/to/pressure-relief-worktree/baml_language
python3 scripts/run_malloc_pressure_relief.py --rate 4000 --duration 300 --interval 1 --final-delay 30
```

The diagnostic build enables GC profiling and every RSS sample also requests `GET /stats`. Each JSONL sample records generation slot occupancy, retained slot capacity, actual TLAB allocations since the last collection, weak heap-permit registry slots, and macOS malloc totals for blocks, live bytes, high-water bytes, and reserved bytes. `tracked_slot_capacity_bytes` covers BAML object slots but excludes payload allocations owned by strings, arrays, maps, and host values; `allocator_bytes_in_use` covers all live malloc allocations in the process, including CPython for the Python target. If BAML slots or permit registrations climb with RSS, the runtime is retaining tracked objects or roots. If they stay flat while allocator live bytes climb, the growth is outside the tracked BAML slots. If allocator live bytes flatten while RSS continues upward, the difference is allocator page retention or non-malloc VM mappings rather than a live malloc leak.

`run_malloc_pressure_relief.py` reproduces the five-minute packed-BAML load, forces one major GC, captures `vmmap -summary`, invokes `malloc_zone_pressure_relief(NULL, 0)`, and records immediate plus delayed RSS and a final VM map. A macOS pack host built with `gc_profiling` installs the opt-in diagnostic only when `BAML_MALLOC_PRESSURE_RELIEF_RESULT` is set: one background thread waits for `SIGUSR2`, performs the relief call, and writes the returned byte count. The probe's fixed startup cost is present throughout the measurement, the executable hash is recorded, and no debugger is loaded into the measured process. The pressure-relief signal occurs after load, outside the throughput interval.

The retained [4,000 RPS pressure-relief run](evidence/2026-09-15-pure-baml-malloc-pressure-relief-4000rps/analysis.json) served 1.2 million requests over five minutes. Explicit GC reduced runtime objects from 204,133 to 15 and malloc live bytes from 50.4 MiB to 19.2 MiB without changing 159.1 MiB RSS or the 164 MiB malloc reservation. Post-GC `vmmap` showed that the 165.3 MiB malloc zone had only 24.7 MiB of dirty physical footprint: 76 MiB was in completely empty small-allocation regions, of which only 0.83 MiB was dirty. Maximal allocator pressure relief returned zero and did not lower RSS because those empty pages were already clean/reclaimable. This classifies the retained floor as allocator high-water address-space and clean-page retention, with about 5.2 MiB of bounded dirty fragmentation, rather than a live-object leak. RSS and reservation were exactly flat during the final 90 loaded seconds, although the original run's late step did not reproduce and five minutes still cannot establish a permanent plateau.

A one-minute proof drove both diagnostic targets at 1,000 RPS in parallel with nine seconds of load followed by a major GC and one-second pause. Both served 54,000 HTTP 200 responses. Pure BAML returned from about 290,000 runtime slots, 18.5 MiB of tracked slot capacity, 9,014 permit registrations, and 63.4 MiB of malloc live data before each mature checkpoint to 15 runtime slots, 0.75 MiB of tracked slot capacity, five permit registrations, and 19.07 MiB of malloc live data after GC. Its RSS still climbed from 82.8 to 99.9 MiB while malloc reserved memory rose and stayed near 108 MiB, directly demonstrating allocator page retention rather than accumulating live BAML memory. Python BAML returned to one runtime slot and four permit registrations after every GC; its post-GC malloc live bytes stayed near 32.8 MiB while RSS settled near 116 MiB and malloc reserved memory stayed near 143 MiB. The retained [allocator-stats proof](evidence/2026-09-14-gc-allocator-stats-proof-1000rps/analysis.json) and [timeline chart](evidence/2026-09-14-gc-allocator-stats-proof-1000rps/gc-allocator-timeline.png) include every checkpoint and the aligned raw sample streams. Regenerate the chart with `uv run --with matplotlib python scripts/plot_gc_allocator_stats.py evidence/2026-09-14-gc-allocator-stats-proof-1000rps`.

The latest-canary sustained-load follow-up rebuilt the diagnostic artifacts from clean revision `540971880764f64a21d23c68703902719efe7c7a`, which is canary `e01f4e1a0ec0996b274ae68f15e5ffdefb977005` plus read-only heap counters, and drove pure BAML and Python BAML concurrently at 2,000 RPS each for 60 continuously loaded seconds. Both returned exactly 120,000 HTTP 200 responses. Pure BAML automatically collected seven times: its tracked objects, weak permits, malloc blocks, and live bytes repeatedly collapsed while RSS flattened at 134.8 MiB with 136 MiB reserved. Python BAML never automatically collected, grew from 98.2 to 204.0 MiB RSS with 240,128 weak permit registrations, then one explicit BAML plus CPython full collection reduced permits to four, malloc live bytes from 137.3 to 36.6 MiB, and RSS to 144.9 MiB. This localizes the main bridge growth to a missing effective automatic-collection trigger for a workload that creates many weak permits but few BAML object slots; the elevated Python post-GC allocator floor remains unresolved. The retained [latest-canary analysis](evidence/2026-09-14-latest-canary-sustained-2000rps/analysis.json) and [timeline chart](evidence/2026-09-14-latest-canary-sustained-2000rps/gc-allocator-timeline.png) contain the full evidence.

A causal follow-up changed only `hello_world`: before returning `"hello world"`, it creates and discards one `baml.http.Response`. At the same 2,000 RPS per target for 60 seconds, Python BAML now reached heap-slot pressure and automatically collected seven times, exactly matching pure BAML's sampled cadence instead of collecting zero times. Its peak weak permit count fell from 236,124 to 32,418, peak malloc-live memory fell from 135.63 to 89.45 MiB, peak reserved capacity fell from 234 to 161 MiB, and loaded RSS peak fell from 202.36 to 162.86 MiB. This confirms that adding BAML object pressure fixes collection starvation and periodically sweeps permits. It does not resolve the process-retention issue: Python's sampled post-automatic-GC RSS floor rose from 138.92 to 161.69 MiB across the seven cycles, malloc block count continued increasing at nearly the baseline rate, and final post-GC RSS was 152.06 MiB versus 144.92 MiB in the baseline. The retained [response-allocation analysis](evidence/2026-09-14-latest-canary-response-allocation-2000rps/analysis.json) and [timeline chart](evidence/2026-09-14-latest-canary-response-allocation-2000rps/gc-allocator-timeline.png) separate the trigger improvement from the unresolved allocator behavior.

The allocation-attribution follow-up drove Python+BAML through a 20-second schedule at 1,000 RPS using 2.5-second load and 0.5-second idle cycles. It ran the `baml.http.Response.new()` workload twice, with and without macOS malloc stack logging, and served 17,000 HTTP 200 responses in each run. The native diagnostic counts all Rust allocations at the PyO3 extension's global allocator, all custom BAML rope nodes and payload bytes, live PyO3 handles, CPython blocks and traced memory, BAML heap generations, process malloc statistics, native threads, RSS, `heap` pointer enumeration, and `vmmap` regions.

The custom rope is not leaking: both runs began and ended with 45 live flat nodes, zero live concat nodes, and 4,786 live payload bytes, while every rope created during the experiment had a matching drop. PyO3 live handles stayed at zero. In the unlogged control, the Rust allocator ended only 102 blocks and 135 KiB above baseline after about 1.186 million allocate/deallocate calls; CPython ended 545 blocks and 86 KiB of traced memory above baseline after full generation-2 collections.

Most importantly, `malloc_zone_statistics().blocks_in_use` is not a reliable live-allocation count for this workload. In the unlogged control it rose by 190,322 blocks, but macOS `heap` independently enumerated only 1,393 additional live pointers and 257 KiB of live allocation bytes. RSS rose 56.1 MiB while native threads increased from 16 to 30 and allocator virtual capacity expanded, then RSS approached a plateau after warm-up. Stack logging substantially perturbed the workload: native threads increased to 108 and tracemalloc bookkeeping alone grew 4.24 MiB. The retained [allocation-attribution analysis](evidence/2026-09-14-python-baml-allocation-attribution-1000rps/analysis.json), summaries, raw samples, `heap` reports, and `vmmap` reports separate actual retained allocations from allocator statistics and observer overhead.

A five-minute pure-BAML follow-up drove the same clean diagnostic build continuously at 4,000 RPS and returned 1,199,996 HTTP 200 responses at 4,000.002 delivered RPS. The run sampled 73 automatic collections at a median 4.00-second interval. Minute-by-minute median malloc-live memory remained stable at 58.07, 60.53, 60.74, 61.22, and 61.14 MiB, while RSS followed stepwise malloc-reserved expansion to 184.25 MiB. Reserved capacity remained exactly 184 MiB for the final 111 seconds, and RSS changed only 0.047 MiB over the final roughly 90 seconds. One final explicit major collection returned the runtime to 15 objects, five weak permit registrations, and 19.18 MiB of malloc-live memory without changing RSS or reserved capacity. This rules out a request-proportional BAML-managed live-object leak over the measured interval, but the process-level result remains suggestive of an allocator-retention or fragmentation leak: reserved capacity grew by 148 MiB despite a stable live-memory envelope and expanded again after more than a minute of apparent stability. The final 90 seconds are too short to establish a permanent plateau. The retained [4,000 RPS analysis](evidence/2026-09-14-latest-canary-pure-4000rps-5min/analysis.json) and [timeline chart](evidence/2026-09-14-latest-canary-pure-4000rps-5min/gc-allocator-timeline.png) contain the raw one-second evidence and derived measurements.

The retained experiment used the clean pre-#4870 parent `42365a45f9d7a3750ed72bd338006d369f6c0e41` plus only the two read-only heap-stat commits from #4837; neither commit changes collection or allocation behavior. It completed 28,124 HTTP 200 requests and ten explicit GC checkpoints. Each complete 30-second burst left 96,143 runtime object slots before GC. Every major collection returned the heap to exactly 15 runtime objects, four active handles, and zero TLAB chunks while taking 4.2–8.4 ms.

RSS did not decrease when the logical objects were reclaimed. It rose from 50.27 MiB before the first GC to 50.84 MiB afterward, reached 53.20 MiB by the third checkpoint, and stayed within 53.20–53.86 MiB through the final checkpoint. The fitted post-GC RSS slope from checkpoints 3–10 was 0.18 MiB/min. This rules out an accumulating live-object set and a collector correctness failure for this workload over five minutes. The earlier pure-BAML RSS curve primarily reflects the automatic collection cadence and allocator pages remaining resident after collection. The retained [analysis](evidence/2026-09-14-pre4870-pure-baml-explicit-gc-100rps/analysis.json) includes every checkpoint; the raw summary, samples, configuration, and build manifest are stored beside it.

At 500 RPS, pure BAML expanded its allocator high-water mark from 89.38 MiB after the first GC to 104.56 MiB after the fourth, then plateaued: checkpoints 4–10 changed by only 0.047 MiB and fit 0.016 MiB/min. Every collection returned the managed heap to exactly 15 runtime objects, four handles, and zero TLAB chunks. Python BAML behaved differently. Even after every pause invoked both a BAML major GC and CPython generation-2 GC, its post-GC RSS rose from 111.39 to 116.97 MiB, fitting 1.38 MiB/min across all checkpoints and 1.51 MiB/min across checkpoints 4–10. The BAML heap had zero runtime objects before each diagnostic call and one response object afterward, while CPython collected 1,459 objects across the ten cycles. This localizes the retained Python-process growth outside the measured BAML heap, but five minutes cannot distinguish a native leak from allocator or runtime caches that plateau later. The retained [500 RPS comparison](evidence/2026-09-14-pre4870-explicit-gc-500rps/analysis.json) includes both raw sample streams and summaries.

A one-minute 2,000 RPS test used 3-second load bursts and one-second GC/idle periods. Pure BAML reached 78.31 MiB after checkpoint 8 and stayed exactly flat through checkpoint 15, with every major GC returning to 15 runtime objects. Python BAML rose from 96.89 to 109.78 MiB post-GC; checkpoints 8–15 added 3.94 MiB and fit 9.46 MiB/min despite both collectors, although its final three checkpoints changed by only 0.08 MiB. This shorter profile again shows pure BAML reaching an allocator plateau and Python retaining memory longer, but it is too short to determine whether Python is starting to plateau. The [2,000 RPS analysis](evidence/2026-09-14-pre4870-explicit-gc-2000rps-3s-on-1s-pause/analysis.json) retains both raw runs.

The three-minute parallel version drove 2,000 RPS into each target simultaneously. Pure BAML handled 266,769 HTTP 200 requests and changed only 0.047 MiB after the first minute, fitting 0.020 MiB/min. Python BAML handled 267,280 HTTP 200 requests and rose by 3.84 MiB after the first minute, but its fitted post-GC slope decelerated from 2.16 MiB/min after 30 seconds to 1.74 after 60 seconds, 1.31 after 90 seconds, and 0.19 over the final 30 seconds. It ended at 95.61 MiB after peaking at 95.72 MiB. This longer run does not show unbounded linear Python growth; its stepwise curve is compatible with a slower allocator or cache plateau, though a longer run or allocation tracing is still needed. The [parallel comparison](evidence/2026-09-14-pre4870-explicit-gc-2000rps-parallel-3min/analysis.json) retains both runs.

## Leak investigation and fix validation

The shared Node and Python slope suggested a request-proportional allocation below both language adapters. A direct `bex_engine` scalar workload confirmed that hypothesis: 100,000 calls allocated no BAML heap slots but increased RSS from 279.53 to 368.64 MiB. Each call created a short-lived VM and registered a weak root holder in `HeapPermitManager::new_permit`. Dead registrations were removed only when `request_park` initiated GC, so a scalar function that never allocated BAML heap objects also never cleaned the registry.

Packed baml-only programs do not use a separate server-specific collector. Their HTTP path invokes each handler through `VmSpawner::spawn_with_callable` in one shared engine, and materializing BAML request and response objects charges the engine's 32 MiB allocation budget. That pressure eventually makes a VM call the ordinary engine collector at a safepoint. The scalar bridge path does not allocate runtime BAML objects, so it never reaches the same trigger even though it creates and retires VM root holders.

The revised candidate extends the engine-owned background worker instead of adding cleanup work to permit allocation. Completed work first gets the existing 100 ms quiet-time collection opportunity. An independent five-second maximum deadline cannot be postponed by continuous calls; when it expires, the worker sets the same `park_requested` flag used by allocation-pressure GC, waits for VMs at their existing safepoints, and runs the same major collection. `HeapPermitManager::new_permit` remains unchanged.

The five-minute end-to-end validation rebuilt both native bridges from clean commit `bc7d504ba456fdb7888d0b3c00a5fade0403941d` and sent 300 RPS independently to every target. Node BAML measured 1.01 MiB/min after warmup versus 0.93 MiB/min for Node baseline, down from 16.46 MiB/min before the fix. Python BAML measured 0.20 MiB/min versus 0.005 MiB/min for Python baseline, down from 16.45 MiB/min. Every BAML target stayed alive, passed every exact-response probe, and returned only HTTP 200 responses. The Node baseline recorded one status `0` among 89,980 attempts, so the runner returned nonzero even though all 89,979 completed baseline requests were HTTP 200. [The retained background-GC summary](evidence/2026-09-14-background-gc-300rps/summary.json) contains the complete measurements. The earlier [permit-sweep experiment](evidence/2026-09-11-holder-sweep-fix-300rps/summary.json) remains as superseded evidence.
