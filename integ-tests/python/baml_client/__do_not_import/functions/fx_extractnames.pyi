# This file is generated by the BAML compiler.
# Do not edit this file directly.
# Instead, edit the BAML files and recompile.

# ruff: noqa: E501,F401
# flake8: noqa: E501,F401
# pylint: disable=unused-import,line-too-long
# fmt: off

from baml_core.stream import AsyncStream
from typing import Callable, List, Protocol, runtime_checkable


import typing

import pytest
from contextlib import contextmanager
from unittest import mock

ImplName = typing.Literal["default_config"]

T = typing.TypeVar("T", bound=typing.Callable[..., typing.Any])
CLS = typing.TypeVar("CLS", bound=type)


IExtractNamesOutput = List[str]

@runtime_checkable
class IExtractNames(Protocol):
    """
    This is the interface for a function.

    Args:
        input: str

    Returns:
        List[str]
    """

    async def __call__(self, *, input: str) -> List[str]:
        ...

   

@runtime_checkable
class IExtractNamesStream(Protocol):
    """
    This is the interface for a stream function.

    Args:
        input: str

    Returns:
        AsyncStream[List[str], List[str]]
    """

    def __call__(self, *, input: str
) -> AsyncStream[List[str], List[str]]:
        ...
class BAMLExtractNamesImpl:
    async def run(self, *, input: str) -> List[str]:
        ...
    
    def stream(self, *, input: str
) -> AsyncStream[List[str], List[str]]:
        ...

class IBAMLExtractNames:
    def register_impl(
        self, name: ImplName
    ) -> typing.Callable[[IExtractNames, IExtractNamesStream], None]:
        ...

    async def __call__(self, *, input: str) -> List[str]:
        ...

    def stream(self, *, input: str
) -> AsyncStream[List[str], List[str]]:
        ...

    def get_impl(self, name: ImplName) -> BAMLExtractNamesImpl:
        ...

    @contextmanager
    def mock(self) -> typing.Generator[mock.AsyncMock, None, None]:
        """
        Utility for mocking the ExtractNamesInterface.

        Usage:
            ```python
            # All implementations are mocked.

            async def test_logic() -> None:
                with baml.ExtractNames.mock() as mocked:
                    mocked.return_value = ...
                    result = await ExtractNamesImpl(...)
                    assert mocked.called
            ```
        """
        ...

    @typing.overload
    def test(self, test_function: T) -> T:
        """
        Provides a pytest.mark.parametrize decorator to facilitate testing different implementations of
        the ExtractNamesInterface.

        Args:
            test_function : T
                The test function to be decorated.

        Usage:
            ```python
            # All implementations will be tested.

            @baml.ExtractNames.test
            async def test_logic(ExtractNamesImpl: IExtractNames) -> None:
                result = await ExtractNamesImpl(...)
            ```
        """
        ...

    @typing.overload
    def test(self, *, exclude_impl: typing.Iterable[ImplName] = [], stream: bool = False) -> pytest.MarkDecorator:
        """
        Provides a pytest.mark.parametrize decorator to facilitate testing different implementations of
        the ExtractNamesInterface.

        Args:
            exclude_impl : Iterable[ImplName]
                The names of the implementations to exclude from testing.
            stream: bool
                If set, will return a streamable version of the test function.

        Usage:
            ```python
            # All implementations except the given impl will be tested.

            @baml.ExtractNames.test(exclude_impl=["implname"])
            async def test_logic(ExtractNamesImpl: IExtractNames) -> None:
                result = await ExtractNamesImpl(...)
            ```

            ```python
            # Streamable version of the test function.

            @baml.ExtractNames.test(stream=True)
            async def test_logic(ExtractNamesImpl: IExtractNamesStream) -> None:
                async for result in ExtractNamesImpl(...):
                    ...
            ```
        """
        ...

    @typing.overload
    def test(self, test_class: typing.Type[CLS]) -> typing.Type[CLS]:
        """
        Provides a pytest.mark.parametrize decorator to facilitate testing different implementations of
        the ExtractNamesInterface.

        Args:
            test_class : Type[CLS]
                The test class to be decorated.

        Usage:
        ```python
        # All implementations will be tested in every test method.

        @baml.ExtractNames.test
        class TestClass:
            def test_a(self, ExtractNamesImpl: IExtractNames) -> None:
                ...
            def test_b(self, ExtractNamesImpl: IExtractNames) -> None:
                ...
        ```
        """
        ...

BAMLExtractNames: IBAMLExtractNames
