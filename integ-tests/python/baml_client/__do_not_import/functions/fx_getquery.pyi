# This file is generated by the BAML compiler.
# Do not edit this file directly.
# Instead, edit the BAML files and recompile.

# ruff: noqa: E501,F401
# flake8: noqa: E501,F401
# pylint: disable=unused-import,line-too-long
# fmt: off

from ..types.classes.cls_searchparams import SearchParams
from ..types.classes.cls_withreasoning import WithReasoning
from ..types.enums.enm_tag import Tag
from ..types.partial.classes.cls_searchparams import PartialSearchParams
from ..types.partial.classes.cls_withreasoning import PartialWithReasoning
from baml_core.stream import AsyncStream
from typing import Callable, Protocol, runtime_checkable


import typing

import pytest
from contextlib import contextmanager
from unittest import mock

ImplName = typing.Literal["default_config"]

T = typing.TypeVar("T", bound=typing.Callable[..., typing.Any])
CLS = typing.TypeVar("CLS", bound=type)


IGetQueryOutput = SearchParams

@runtime_checkable
class IGetQuery(Protocol):
    """
    This is the interface for a function.

    Args:
        query: str

    Returns:
        SearchParams
    """

    async def __call__(self, *, query: str) -> SearchParams:
        ...

   

@runtime_checkable
class IGetQueryStream(Protocol):
    """
    This is the interface for a stream function.

    Args:
        query: str

    Returns:
        AsyncStream[SearchParams, PartialSearchParams]
    """

    def __call__(self, *, query: str
) -> AsyncStream[SearchParams, PartialSearchParams]:
        ...
class BAMLGetQueryImpl:
    async def run(self, *, query: str) -> SearchParams:
        ...
    
    def stream(self, *, query: str
) -> AsyncStream[SearchParams, PartialSearchParams]:
        ...

class IBAMLGetQuery:
    def register_impl(
        self, name: ImplName
    ) -> typing.Callable[[IGetQuery, IGetQueryStream], None]:
        ...

    async def __call__(self, *, query: str) -> SearchParams:
        ...

    def stream(self, *, query: str
) -> AsyncStream[SearchParams, PartialSearchParams]:
        ...

    def get_impl(self, name: ImplName) -> BAMLGetQueryImpl:
        ...

    @contextmanager
    def mock(self) -> typing.Generator[mock.AsyncMock, None, None]:
        """
        Utility for mocking the GetQueryInterface.

        Usage:
            ```python
            # All implementations are mocked.

            async def test_logic() -> None:
                with baml.GetQuery.mock() as mocked:
                    mocked.return_value = ...
                    result = await GetQueryImpl(...)
                    assert mocked.called
            ```
        """
        ...

    @typing.overload
    def test(self, test_function: T) -> T:
        """
        Provides a pytest.mark.parametrize decorator to facilitate testing different implementations of
        the GetQueryInterface.

        Args:
            test_function : T
                The test function to be decorated.

        Usage:
            ```python
            # All implementations will be tested.

            @baml.GetQuery.test
            async def test_logic(GetQueryImpl: IGetQuery) -> None:
                result = await GetQueryImpl(...)
            ```
        """
        ...

    @typing.overload
    def test(self, *, exclude_impl: typing.Iterable[ImplName] = [], stream: bool = False) -> pytest.MarkDecorator:
        """
        Provides a pytest.mark.parametrize decorator to facilitate testing different implementations of
        the GetQueryInterface.

        Args:
            exclude_impl : Iterable[ImplName]
                The names of the implementations to exclude from testing.
            stream: bool
                If set, will return a streamable version of the test function.

        Usage:
            ```python
            # All implementations except the given impl will be tested.

            @baml.GetQuery.test(exclude_impl=["implname"])
            async def test_logic(GetQueryImpl: IGetQuery) -> None:
                result = await GetQueryImpl(...)
            ```

            ```python
            # Streamable version of the test function.

            @baml.GetQuery.test(stream=True)
            async def test_logic(GetQueryImpl: IGetQueryStream) -> None:
                async for result in GetQueryImpl(...):
                    ...
            ```
        """
        ...

    @typing.overload
    def test(self, test_class: typing.Type[CLS]) -> typing.Type[CLS]:
        """
        Provides a pytest.mark.parametrize decorator to facilitate testing different implementations of
        the GetQueryInterface.

        Args:
            test_class : Type[CLS]
                The test class to be decorated.

        Usage:
        ```python
        # All implementations will be tested in every test method.

        @baml.GetQuery.test
        class TestClass:
            def test_a(self, GetQueryImpl: IGetQuery) -> None:
                ...
            def test_b(self, GetQueryImpl: IGetQuery) -> None:
                ...
        ```
        """
        ...

BAMLGetQuery: IBAMLGetQuery
