# This file is generated by the BAML compiler.
# Do not edit this file directly.
# Instead, edit the BAML files and recompile.

# ruff: noqa: E501,F401
# flake8: noqa: E501,F401
# pylint: disable=unused-import,line-too-long
# fmt: off

from ..types.enums.enm_enumoutput import EnumOutput
from baml_core.stream import AsyncStream
from typing import Callable, Protocol, runtime_checkable


import typing

import pytest
from contextlib import contextmanager
from unittest import mock

ImplName = typing.Literal["v1"]

T = typing.TypeVar("T", bound=typing.Callable[..., typing.Any])
CLS = typing.TypeVar("CLS", bound=type)


IFnEnumOutputOutput = EnumOutput

@runtime_checkable
class IFnEnumOutput(Protocol):
    """
    This is the interface for a function.

    Args:
        arg: str

    Returns:
        EnumOutput
    """

    async def __call__(self, arg: str, /) -> EnumOutput:
        ...

   

@runtime_checkable
class IFnEnumOutputStream(Protocol):
    """
    This is the interface for a stream function.

    Args:
        arg: str

    Returns:
        AsyncStream[EnumOutput, EnumOutput]
    """

    def __call__(self, arg: str, /) -> AsyncStream[EnumOutput, EnumOutput]:
        ...
class BAMLFnEnumOutputImpl:
    async def run(self, arg: str, /) -> EnumOutput:
        ...
    
    def stream(self, arg: str, /) -> AsyncStream[EnumOutput, EnumOutput]:
        ...

class IBAMLFnEnumOutput:
    def register_impl(
        self, name: ImplName
    ) -> typing.Callable[[IFnEnumOutput, IFnEnumOutputStream], None]:
        ...

    async def __call__(self, arg: str, /) -> EnumOutput:
        ...

    def stream(self, arg: str, /) -> AsyncStream[EnumOutput, EnumOutput]:
        ...

    def get_impl(self, name: ImplName) -> BAMLFnEnumOutputImpl:
        ...

    @contextmanager
    def mock(self) -> typing.Generator[mock.AsyncMock, None, None]:
        """
        Utility for mocking the FnEnumOutputInterface.

        Usage:
            ```python
            # All implementations are mocked.

            async def test_logic() -> None:
                with baml.FnEnumOutput.mock() as mocked:
                    mocked.return_value = ...
                    result = await FnEnumOutputImpl(...)
                    assert mocked.called
            ```
        """
        ...

    @typing.overload
    def test(self, test_function: T) -> T:
        """
        Provides a pytest.mark.parametrize decorator to facilitate testing different implementations of
        the FnEnumOutputInterface.

        Args:
            test_function : T
                The test function to be decorated.

        Usage:
            ```python
            # All implementations will be tested.

            @baml.FnEnumOutput.test
            async def test_logic(FnEnumOutputImpl: IFnEnumOutput) -> None:
                result = await FnEnumOutputImpl(...)
            ```
        """
        ...

    @typing.overload
    def test(self, *, exclude_impl: typing.Iterable[ImplName] = [], stream: bool = False) -> pytest.MarkDecorator:
        """
        Provides a pytest.mark.parametrize decorator to facilitate testing different implementations of
        the FnEnumOutputInterface.

        Args:
            exclude_impl : Iterable[ImplName]
                The names of the implementations to exclude from testing.
            stream: bool
                If set, will return a streamable version of the test function.

        Usage:
            ```python
            # All implementations except the given impl will be tested.

            @baml.FnEnumOutput.test(exclude_impl=["implname"])
            async def test_logic(FnEnumOutputImpl: IFnEnumOutput) -> None:
                result = await FnEnumOutputImpl(...)
            ```

            ```python
            # Streamable version of the test function.

            @baml.FnEnumOutput.test(stream=True)
            async def test_logic(FnEnumOutputImpl: IFnEnumOutputStream) -> None:
                async for result in FnEnumOutputImpl(...):
                    ...
            ```
        """
        ...

    @typing.overload
    def test(self, test_class: typing.Type[CLS]) -> typing.Type[CLS]:
        """
        Provides a pytest.mark.parametrize decorator to facilitate testing different implementations of
        the FnEnumOutputInterface.

        Args:
            test_class : Type[CLS]
                The test class to be decorated.

        Usage:
        ```python
        # All implementations will be tested in every test method.

        @baml.FnEnumOutput.test
        class TestClass:
            def test_a(self, FnEnumOutputImpl: IFnEnumOutput) -> None:
                ...
            def test_b(self, FnEnumOutputImpl: IFnEnumOutput) -> None:
                ...
        ```
        """
        ...

BAMLFnEnumOutput: IBAMLFnEnumOutput
