# This file is generated by the BAML compiler.
# Do not edit this file directly.
# Instead, edit the BAML files and recompile.

# ruff: noqa: E501,F401
# flake8: noqa: E501,F401
# pylint: disable=unused-import,line-too-long
# fmt: off

from ..types.classes.cls_resume import Resume
from ..types.partial.classes.cls_resume import PartialResume
from baml_core.stream import AsyncStream
from typing import Callable, Protocol, runtime_checkable


import typing

import pytest
from contextlib import contextmanager
from unittest import mock

ImplName = typing.Literal["default_config"]

T = typing.TypeVar("T", bound=typing.Callable[..., typing.Any])
CLS = typing.TypeVar("CLS", bound=type)


IExtractResume2Output = Resume

@runtime_checkable
class IExtractResume2(Protocol):
    """
    This is the interface for a function.

    Args:
        resume: str

    Returns:
        Resume
    """

    async def __call__(self, *, resume: str) -> Resume:
        ...

   

@runtime_checkable
class IExtractResume2Stream(Protocol):
    """
    This is the interface for a stream function.

    Args:
        resume: str

    Returns:
        AsyncStream[Resume, PartialResume]
    """

    def __call__(self, *, resume: str
) -> AsyncStream[Resume, PartialResume]:
        ...
class BAMLExtractResume2Impl:
    async def run(self, *, resume: str) -> Resume:
        ...
    
    def stream(self, *, resume: str
) -> AsyncStream[Resume, PartialResume]:
        ...

class IBAMLExtractResume2:
    def register_impl(
        self, name: ImplName
    ) -> typing.Callable[[IExtractResume2, IExtractResume2Stream], None]:
        ...

    async def __call__(self, *, resume: str) -> Resume:
        ...

    def stream(self, *, resume: str
) -> AsyncStream[Resume, PartialResume]:
        ...

    def get_impl(self, name: ImplName) -> BAMLExtractResume2Impl:
        ...

    @contextmanager
    def mock(self) -> typing.Generator[mock.AsyncMock, None, None]:
        """
        Utility for mocking the ExtractResume2Interface.

        Usage:
            ```python
            # All implementations are mocked.

            async def test_logic() -> None:
                with baml.ExtractResume2.mock() as mocked:
                    mocked.return_value = ...
                    result = await ExtractResume2Impl(...)
                    assert mocked.called
            ```
        """
        ...

    @typing.overload
    def test(self, test_function: T) -> T:
        """
        Provides a pytest.mark.parametrize decorator to facilitate testing different implementations of
        the ExtractResume2Interface.

        Args:
            test_function : T
                The test function to be decorated.

        Usage:
            ```python
            # All implementations will be tested.

            @baml.ExtractResume2.test
            async def test_logic(ExtractResume2Impl: IExtractResume2) -> None:
                result = await ExtractResume2Impl(...)
            ```
        """
        ...

    @typing.overload
    def test(self, *, exclude_impl: typing.Iterable[ImplName] = [], stream: bool = False) -> pytest.MarkDecorator:
        """
        Provides a pytest.mark.parametrize decorator to facilitate testing different implementations of
        the ExtractResume2Interface.

        Args:
            exclude_impl : Iterable[ImplName]
                The names of the implementations to exclude from testing.
            stream: bool
                If set, will return a streamable version of the test function.

        Usage:
            ```python
            # All implementations except the given impl will be tested.

            @baml.ExtractResume2.test(exclude_impl=["implname"])
            async def test_logic(ExtractResume2Impl: IExtractResume2) -> None:
                result = await ExtractResume2Impl(...)
            ```

            ```python
            # Streamable version of the test function.

            @baml.ExtractResume2.test(stream=True)
            async def test_logic(ExtractResume2Impl: IExtractResume2Stream) -> None:
                async for result in ExtractResume2Impl(...):
                    ...
            ```
        """
        ...

    @typing.overload
    def test(self, test_class: typing.Type[CLS]) -> typing.Type[CLS]:
        """
        Provides a pytest.mark.parametrize decorator to facilitate testing different implementations of
        the ExtractResume2Interface.

        Args:
            test_class : Type[CLS]
                The test class to be decorated.

        Usage:
        ```python
        # All implementations will be tested in every test method.

        @baml.ExtractResume2.test
        class TestClass:
            def test_a(self, ExtractResume2Impl: IExtractResume2) -> None:
                ...
            def test_b(self, ExtractResume2Impl: IExtractResume2) -> None:
                ...
        ```
        """
        ...

BAMLExtractResume2: IBAMLExtractResume2
