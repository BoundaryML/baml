# This file is generated by the BAML compiler.
# Do not edit this file directly.
# Instead, edit the BAML files and recompile.

# ruff: noqa: E501,F401
# flake8: noqa: E501,F401
# pylint: disable=unused-import,line-too-long
# fmt: off

from ..types.classes.cls_message import Message
from ..types.enums.enm_category import Category
from ..types.partial.classes.cls_message import PartialMessage
from baml_core.stream import AsyncStream
from typing import Callable, List, Protocol, runtime_checkable


import typing

import pytest
from contextlib import contextmanager
from unittest import mock

ImplName = typing.Literal["default_config"]

T = typing.TypeVar("T", bound=typing.Callable[..., typing.Any])
CLS = typing.TypeVar("CLS", bound=type)


IClassifyConversationOutput = List[Category]

@runtime_checkable
class IClassifyConversation(Protocol):
    """
    This is the interface for a function.

    Args:
        messages: List[Message]

    Returns:
        List[Category]
    """

    async def __call__(self, *, messages: List[Message]) -> List[Category]:
        ...

   

@runtime_checkable
class IClassifyConversationStream(Protocol):
    """
    This is the interface for a stream function.

    Args:
        messages: List[Message]

    Returns:
        AsyncStream[List[Category], List[Category]]
    """

    def __call__(self, *, messages: List[Message]
) -> AsyncStream[List[Category], List[Category]]:
        ...
class BAMLClassifyConversationImpl:
    async def run(self, *, messages: List[Message]) -> List[Category]:
        ...
    
    def stream(self, *, messages: List[Message]
) -> AsyncStream[List[Category], List[Category]]:
        ...

class IBAMLClassifyConversation:
    def register_impl(
        self, name: ImplName
    ) -> typing.Callable[[IClassifyConversation, IClassifyConversationStream], None]:
        ...

    async def __call__(self, *, messages: List[Message]) -> List[Category]:
        ...

    def stream(self, *, messages: List[Message]
) -> AsyncStream[List[Category], List[Category]]:
        ...

    def get_impl(self, name: ImplName) -> BAMLClassifyConversationImpl:
        ...

    @contextmanager
    def mock(self) -> typing.Generator[mock.AsyncMock, None, None]:
        """
        Utility for mocking the ClassifyConversationInterface.

        Usage:
            ```python
            # All implementations are mocked.

            async def test_logic() -> None:
                with baml.ClassifyConversation.mock() as mocked:
                    mocked.return_value = ...
                    result = await ClassifyConversationImpl(...)
                    assert mocked.called
            ```
        """
        ...

    @typing.overload
    def test(self, test_function: T) -> T:
        """
        Provides a pytest.mark.parametrize decorator to facilitate testing different implementations of
        the ClassifyConversationInterface.

        Args:
            test_function : T
                The test function to be decorated.

        Usage:
            ```python
            # All implementations will be tested.

            @baml.ClassifyConversation.test
            async def test_logic(ClassifyConversationImpl: IClassifyConversation) -> None:
                result = await ClassifyConversationImpl(...)
            ```
        """
        ...

    @typing.overload
    def test(self, *, exclude_impl: typing.Iterable[ImplName] = [], stream: bool = False) -> pytest.MarkDecorator:
        """
        Provides a pytest.mark.parametrize decorator to facilitate testing different implementations of
        the ClassifyConversationInterface.

        Args:
            exclude_impl : Iterable[ImplName]
                The names of the implementations to exclude from testing.
            stream: bool
                If set, will return a streamable version of the test function.

        Usage:
            ```python
            # All implementations except the given impl will be tested.

            @baml.ClassifyConversation.test(exclude_impl=["implname"])
            async def test_logic(ClassifyConversationImpl: IClassifyConversation) -> None:
                result = await ClassifyConversationImpl(...)
            ```

            ```python
            # Streamable version of the test function.

            @baml.ClassifyConversation.test(stream=True)
            async def test_logic(ClassifyConversationImpl: IClassifyConversationStream) -> None:
                async for result in ClassifyConversationImpl(...):
                    ...
            ```
        """
        ...

    @typing.overload
    def test(self, test_class: typing.Type[CLS]) -> typing.Type[CLS]:
        """
        Provides a pytest.mark.parametrize decorator to facilitate testing different implementations of
        the ClassifyConversationInterface.

        Args:
            test_class : Type[CLS]
                The test class to be decorated.

        Usage:
        ```python
        # All implementations will be tested in every test method.

        @baml.ClassifyConversation.test
        class TestClass:
            def test_a(self, ClassifyConversationImpl: IClassifyConversation) -> None:
                ...
            def test_b(self, ClassifyConversationImpl: IClassifyConversation) -> None:
                ...
        ```
        """
        ...

BAMLClassifyConversation: IBAMLClassifyConversation
