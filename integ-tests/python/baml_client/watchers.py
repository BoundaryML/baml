# ----------------------------------------------------------------------------
#
#  Welcome to Baml! To use this generated code, please run the following:
#
#  $ pip install baml
#
# ----------------------------------------------------------------------------

# This file was generated by BAML: please do not edit it. Instead, edit the
# BAML files and re-generate this code using: baml-cli generate
# baml-cli is available with the baml package.

from typing import Callable, Any, Protocol, Generic, TypeVar, overload, Literal
import threading

T = TypeVar("T")

class BlockEvent:
    def __init__(self, block_label: str, event_type: str):
        self.block_label = block_label
        self.event_type = event_type  # "enter" | "exit"

class VarEvent(Generic[T]):
    def __init__(self, variable_name: str, value: T, timestamp: str, function_name: str):
        self.variable_name = variable_name
        self.value = value
        self.timestamp = timestamp
        self.function_name = function_name

BlockHandler = Callable[[BlockEvent], None]
VarEventHandler = Callable[[VarEvent[T]], None]
StreamHandler = Callable[[Any], None]  # Stream will be an async iterator

class InternalEventBindings(Protocol):
    function_name: str
    block: list[BlockHandler]
    vars: dict[str, list[VarEventHandler[Any]]]
    streams: dict[str, list[StreamHandler]]
    functions: dict[str, "InternalEventBindings"]

class EventCollectorInternal(Protocol):
    def __handlers__(self) -> InternalEventBindings:
        ...


class AllowedOptionalsExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "AllowedOptionalsExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def AllowedOptionalsExprFunc() -> AllowedOptionalsExprFuncEventCollector:
    return AllowedOptionalsExprFuncEventCollector()


class AnotherTakedownEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "AnotherTakedown"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def AnotherTakedown() -> AnotherTakedownEventCollector:
    return AnotherTakedownEventCollector()


class AssignElseIfExprEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "AssignElseIfExpr"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def AssignElseIfExpr() -> AssignElseIfExprEventCollector:
    return AssignElseIfExprEventCollector()


class AudioInputExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "AudioInputExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def AudioInputExprFunc() -> AudioInputExprFuncEventCollector:
    return AudioInputExprFuncEventCollector()


class BoolToIntWithIfElseEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "BoolToIntWithIfElse"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def BoolToIntWithIfElse() -> BoolToIntWithIfElseEventCollector:
    return BoolToIntWithIfElseEventCollector()


class BoolToIntWithIfElseCallingLlmEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "BoolToIntWithIfElseCallingLlm"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def BoolToIntWithIfElseCallingLlm() -> BoolToIntWithIfElseCallingLlmEventCollector:
    return BoolToIntWithIfElseCallingLlmEventCollector()


class CallLlmDescribeImageEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "CallLlmDescribeImage"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def CallLlmDescribeImage() -> CallLlmDescribeImageEventCollector:
    return CallLlmDescribeImageEventCollector()


class CallReturnOneEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "CallReturnOne"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def CallReturnOne() -> CallReturnOneEventCollector:
    return CallReturnOneEventCollector()


class ChainedCallsEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "ChainedCalls"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def ChainedCalls() -> ChainedCallsEventCollector:
    return ChainedCallsEventCollector()


class EchoWorkflowEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "EchoWorkflow"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def EchoWorkflow() -> EchoWorkflowEventCollector:
    return EchoWorkflowEventCollector()


class FnLiteralClassInputOutputExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "FnLiteralClassInputOutputExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def FnLiteralClassInputOutputExprFunc() -> FnLiteralClassInputOutputExprFuncEventCollector:
    return FnLiteralClassInputOutputExprFuncEventCollector()


class FnLiteralUnionClassInputOutputExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "FnLiteralUnionClassInputOutputExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def FnLiteralUnionClassInputOutputExprFunc() -> FnLiteralUnionClassInputOutputExprFuncEventCollector:
    return FnLiteralUnionClassInputOutputExprFuncEventCollector()


class FnNamedArgsSingleStringOptionalExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "FnNamedArgsSingleStringOptionalExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def FnNamedArgsSingleStringOptionalExprFunc() -> FnNamedArgsSingleStringOptionalExprFuncEventCollector:
    return FnNamedArgsSingleStringOptionalExprFuncEventCollector()


class FnOutputBoolExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "FnOutputBoolExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def FnOutputBoolExprFunc() -> FnOutputBoolExprFuncEventCollector:
    return FnOutputBoolExprFuncEventCollector()


class FnOutputClassExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "FnOutputClassExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def FnOutputClassExprFunc() -> FnOutputClassExprFuncEventCollector:
    return FnOutputClassExprFuncEventCollector()


class FnOutputClassListExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "FnOutputClassListExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def FnOutputClassListExprFunc() -> FnOutputClassListExprFuncEventCollector:
    return FnOutputClassListExprFuncEventCollector()


class FnOutputClassNestedExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "FnOutputClassNestedExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def FnOutputClassNestedExprFunc() -> FnOutputClassNestedExprFuncEventCollector:
    return FnOutputClassNestedExprFuncEventCollector()


class FnOutputIntExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "FnOutputIntExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def FnOutputIntExprFunc() -> FnOutputIntExprFuncEventCollector:
    return FnOutputIntExprFuncEventCollector()


class FnOutputLiteralStringExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "FnOutputLiteralStringExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def FnOutputLiteralStringExprFunc() -> FnOutputLiteralStringExprFuncEventCollector:
    return FnOutputLiteralStringExprFuncEventCollector()


class FnTestNamedArgsSingleEnumExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "FnTestNamedArgsSingleEnumExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def FnTestNamedArgsSingleEnumExprFunc() -> FnTestNamedArgsSingleEnumExprFuncEventCollector:
    return FnTestNamedArgsSingleEnumExprFuncEventCollector()


class IsTargetWordEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "IsTargetWord"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def IsTargetWord() -> IsTargetWordEventCollector:
    return IsTargetWordEventCollector()


class IsTargetWord2EventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "IsTargetWord2"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def IsTargetWord2() -> IsTargetWord2EventCollector:
    return IsTargetWord2EventCollector()


class IterativeFibonacciEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "IterativeFibonacci"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def IterativeFibonacci() -> IterativeFibonacciEventCollector:
    return IterativeFibonacciEventCollector()


class NormalElseIfStmtEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "NormalElseIfStmt"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def NormalElseIfStmt() -> NormalElseIfStmtEventCollector:
    return NormalElseIfStmtEventCollector()


class NotEmptyEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "NotEmpty"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def NotEmpty() -> NotEmptyEventCollector:
    return NotEmptyEventCollector()


class NullLiteralClassHelloExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "NullLiteralClassHelloExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def NullLiteralClassHelloExprFunc() -> NullLiteralClassHelloExprFuncEventCollector:
    return NullLiteralClassHelloExprFuncEventCollector()


class PdfInputExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "PdfInputExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def PdfInputExprFunc() -> PdfInputExprFuncEventCollector:
    return PdfInputExprFuncEventCollector()


class ReturnCategoryEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "ReturnCategory"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def ReturnCategory() -> ReturnCategoryEventCollector:
    return ReturnCategoryEventCollector()


class ReturnElseIfExprEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "ReturnElseIfExpr"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def ReturnElseIfExpr() -> ReturnElseIfExprEventCollector:
    return ReturnElseIfExprEventCollector()


class ReturnNumberEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "ReturnNumber"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def ReturnNumber() -> ReturnNumberEventCollector:
    return ReturnNumberEventCollector()


class ReturnNumberCallingLlmEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "ReturnNumberCallingLlm"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def ReturnNumberCallingLlm() -> ReturnNumberCallingLlmEventCollector:
    return ReturnNumberCallingLlmEventCollector()


class ReturnOneEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "ReturnOne"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def ReturnOne() -> ReturnOneEventCollector:
    return ReturnOneEventCollector()


class StoreFnCallInLocalVarEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "StoreFnCallInLocalVar"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def StoreFnCallInLocalVar() -> StoreFnCallInLocalVarEventCollector:
    return StoreFnCallInLocalVarEventCollector()


class StoreLlmCallInLocalVarEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "StoreLlmCallInLocalVar"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def StoreLlmCallInLocalVar() -> StoreLlmCallInLocalVarEventCollector:
    return StoreLlmCallInLocalVarEventCollector()


class SumArrayEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "SumArray"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def SumArray() -> SumArrayEventCollector:
    return SumArrayEventCollector()


class SumFromToEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "SumFromTo"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def SumFromTo() -> SumFromToEventCollector:
    return SumFromToEventCollector()


class TestFnNamedArgsLiteralBoolExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "TestFnNamedArgsLiteralBoolExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def TestFnNamedArgsLiteralBoolExprFunc() -> TestFnNamedArgsLiteralBoolExprFuncEventCollector:
    return TestFnNamedArgsLiteralBoolExprFuncEventCollector()


class TestFnNamedArgsLiteralIntExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "TestFnNamedArgsLiteralIntExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def TestFnNamedArgsLiteralIntExprFunc() -> TestFnNamedArgsLiteralIntExprFuncEventCollector:
    return TestFnNamedArgsLiteralIntExprFuncEventCollector()


class TestFnNamedArgsLiteralStringExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "TestFnNamedArgsLiteralStringExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def TestFnNamedArgsLiteralStringExprFunc() -> TestFnNamedArgsLiteralStringExprFuncEventCollector:
    return TestFnNamedArgsLiteralStringExprFuncEventCollector()


class TestFnNamedArgsSingleBoolExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "TestFnNamedArgsSingleBoolExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def TestFnNamedArgsSingleBoolExprFunc() -> TestFnNamedArgsSingleBoolExprFuncEventCollector:
    return TestFnNamedArgsSingleBoolExprFuncEventCollector()


class TestFnNamedArgsSingleClassExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "TestFnNamedArgsSingleClassExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def TestFnNamedArgsSingleClassExprFunc() -> TestFnNamedArgsSingleClassExprFuncEventCollector:
    return TestFnNamedArgsSingleClassExprFuncEventCollector()


class TestFnNamedArgsSingleEnumListExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "TestFnNamedArgsSingleEnumListExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def TestFnNamedArgsSingleEnumListExprFunc() -> TestFnNamedArgsSingleEnumListExprFuncEventCollector:
    return TestFnNamedArgsSingleEnumListExprFuncEventCollector()


class TestFnNamedArgsSingleFloatExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "TestFnNamedArgsSingleFloatExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def TestFnNamedArgsSingleFloatExprFunc() -> TestFnNamedArgsSingleFloatExprFuncEventCollector:
    return TestFnNamedArgsSingleFloatExprFuncEventCollector()


class TestFnNamedArgsSingleIntExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "TestFnNamedArgsSingleIntExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def TestFnNamedArgsSingleIntExprFunc() -> TestFnNamedArgsSingleIntExprFuncEventCollector:
    return TestFnNamedArgsSingleIntExprFuncEventCollector()


class TestFnNamedArgsSingleMapStringToClassExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "TestFnNamedArgsSingleMapStringToClassExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def TestFnNamedArgsSingleMapStringToClassExprFunc() -> TestFnNamedArgsSingleMapStringToClassExprFuncEventCollector:
    return TestFnNamedArgsSingleMapStringToClassExprFuncEventCollector()


class TestFnNamedArgsSingleMapStringToMapExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "TestFnNamedArgsSingleMapStringToMapExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def TestFnNamedArgsSingleMapStringToMapExprFunc() -> TestFnNamedArgsSingleMapStringToMapExprFuncEventCollector:
    return TestFnNamedArgsSingleMapStringToMapExprFuncEventCollector()


class TestFnNamedArgsSingleMapStringToStringExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "TestFnNamedArgsSingleMapStringToStringExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def TestFnNamedArgsSingleMapStringToStringExprFunc() -> TestFnNamedArgsSingleMapStringToStringExprFuncEventCollector:
    return TestFnNamedArgsSingleMapStringToStringExprFuncEventCollector()


class TestFnNamedArgsSingleStringArrayExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "TestFnNamedArgsSingleStringArrayExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def TestFnNamedArgsSingleStringArrayExprFunc() -> TestFnNamedArgsSingleStringArrayExprFuncEventCollector:
    return TestFnNamedArgsSingleStringArrayExprFuncEventCollector()


class TestFnNamedArgsSingleStringExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "TestFnNamedArgsSingleStringExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def TestFnNamedArgsSingleStringExprFunc() -> TestFnNamedArgsSingleStringExprFuncEventCollector:
    return TestFnNamedArgsSingleStringExprFuncEventCollector()


class TestFnNamedArgsSingleStringListExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "TestFnNamedArgsSingleStringListExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def TestFnNamedArgsSingleStringListExprFunc() -> TestFnNamedArgsSingleStringListExprFuncEventCollector:
    return TestFnNamedArgsSingleStringListExprFuncEventCollector()


class TestImageInputExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "TestImageInputExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def TestImageInputExprFunc() -> TestImageInputExprFuncEventCollector:
    return TestImageInputExprFuncEventCollector()


class TestImageListInputExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "TestImageListInputExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def TestImageListInputExprFunc() -> TestImageListInputExprFuncEventCollector:
    return TestImageListInputExprFuncEventCollector()


class VideoInputExprFuncEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {}
        self._stream_handler_map: dict[str, list[StreamHandler]] = {}
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "VideoInputExprFunc"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def VideoInputExprFunc() -> VideoInputExprFuncEventCollector:
    return VideoInputExprFuncEventCollector()


class WorkflowWatchChildEventCollector:
    def __init__(self):
        self._block_handlers: list[BlockHandler] = []
        self._lock = threading.Lock()

        
        self._var_handlers_x: list[VarEventHandler[str]] = []
        self._stream_handlers_x: list[StreamHandler] = []
        

        
        self._var_handler_map: dict[str, list[VarEventHandler[Any]]] = {
            
            "x": self._var_handlers_x,
            
        }

        self._stream_handler_map: dict[str, list[StreamHandler]] = {
            
            "x": self._stream_handlers_x,
            
        }
        

        
        self._function_handler_map: dict[str, EventCollectorInternal] = {}
        

        

    def on_block(self, handler: BlockHandler) -> None:
        with self._lock:
            self._block_handlers.append(handler)

    
    
    @overload
    def on_var(self, channel: Literal["x"], handler: VarEventHandler[str]) -> None: ...
    

    def on_var(self, channel: Literal["x"], handler: VarEventHandler[Any]) -> None:
        with self._lock:
            if channel in self._var_handler_map:
                self._var_handler_map[channel].append(handler)

    
    @overload
    def on_stream(self, channel: Literal["x"], handler: StreamHandler) -> None: ...
    

    def on_stream(self, channel: Literal["x"], handler: StreamHandler) -> None:
        with self._lock:
            if channel in self._stream_handler_map:
                self._stream_handler_map[channel].append(handler)
    

    def __handlers__(self) -> InternalEventBindings:
        with self._lock:
            vars_dict = {
                channel: list(handlers)
                for channel, handlers in self._var_handler_map.items()
                if handlers
            }

            streams_dict = {
                channel: list(handlers)
                for channel, handlers in self._stream_handler_map.items()
                if handlers
            }

            functions_dict = {
                fn_name: collector.__handlers__()
                for fn_name, collector in self._function_handler_map.items()
            }

            # Capture block_handlers from outer scope
            block_handlers = list(self._block_handlers)

            class Bindings:
                def __init__(self):
                    self.function_name = "WorkflowWatchChild"
                    self.block = block_handlers
                    self.vars = vars_dict
                    self.streams = streams_dict
                    self.functions = functions_dict

            return Bindings()

def WorkflowWatchChild() -> WorkflowWatchChildEventCollector:
    return WorkflowWatchChildEventCollector()

