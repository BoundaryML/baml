/*************************************************************************************************

Welcome to Baml! To use this generated code, please run one of the following:

$ npm install @boundaryml/baml
$ yarn add @boundaryml/baml
$ pnpm add @boundaryml/baml

*************************************************************************************************/

// This file was generated by BAML: do not edit it. Instead, edit the BAML
// files and re-generate this code.
//
/* eslint-disable */
// tslint:disable
// @ts-nocheck
// biome-ignore format: autogenerated code
'use server'

import { b } from '../index';
import type { Check, Checked  } from "../types";
import type { Image, Audio } from "@boundaryml/baml";

import type {  AliasedEnum,  AnotherObject,  BigNumbers,  BinaryNode,  Blah,  BlockConstraint,  BlockConstraintForParam,  BookOrder,  Category,  Category2,  Category3,  ClassForNullLiteral,  ClassOptionalOutput,  ClassOptionalOutput2,  ClassToRecAlias,  ClassWithBlockDone,  ClassWithImage,  ClassWithoutDone,  Color,  ComplexMemoryObject,  CompoundBigNumbers,  ContactInfo,  CustomTaskResult,  DataType,  DummyOutput,  DynEnumOne,  DynEnumTwo,  DynInputOutput,  DynamicClassOne,  DynamicClassTwo,  DynamicOutput,  Earthling,  Education,  Email,  EmailAddress,  EnumInClass,  EnumOutput,  Event,  FakeImage,  FlightConfirmation,  FooAny,  Forest,  FormatterTest0,  FormatterTest1,  FormatterTest2,  FormatterTest3,  GroceryReceipt,  Hobby,  InnerClass,  InnerClass2,  InputClass,  InputClassNested,  JsonArray,  JsonEntry,  JsonObject,  JsonTemplate,  JsonValue,  LinkedList,  LinkedListAliasNode,  LiteralClassHello,  LiteralClassOne,  LiteralClassTwo,  MalformedConstraints,  MalformedConstraints2,  MapKey,  Martian,  MemoryObject,  MergeAttrs,  NamedArgsSingleClass,  NamedArgsSingleEnum,  NamedArgsSingleEnumList,  Nested,  Nested2,  NestedBlockConstraint,  NestedBlockConstraintForParam,  Node,  NodeWithAliasIndirection,  OptionalListAndMap,  OptionalTest_CategoryType,  OptionalTest_Prop1,  OptionalTest_ReturnType,  OrderInfo,  OrderStatus,  OriginalA,  OriginalB,  Person,  PhoneNumber,  Quantity,  RaysData,  RecAliasOne,  RecAliasThree,  RecAliasTwo,  ReceiptInfo,  ReceiptItem,  Recipe,  RecursiveAliasDependency,  RecursiveListAlias,  RecursiveMapAlias,  Resume,  Schema,  SearchParams,  SemanticContainer,  SimpleTag,  SmallThing,  SomeClassNestedDynamic,  StringToClassEntry,  Tag,  TestClassAlias,  TestClassNested,  TestClassWithEnum,  TestEnum,  TestMemoryOutput,  TestOutputClass,  Tree,  TwoStoriesOneTitle,  UnionTest_ReturnType,  UniverseQuestion,  UniverseQuestionInput,  WithReasoning } from "../types"

import type * as types from "../types"

/**
 * Streaming BAML server actions that return ReadableStreams.
 */

/**
 * Executes the streaming variant of the "AaaSamOutputFormat" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } recipe - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const AaaSamOutputFormat = async (
  recipe: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.AaaSamOutputFormat(
    recipe,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "AliasThatPointsToRecursiveType" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { LinkedListAliasNode } list - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const AliasThatPointsToRecursiveType = async (
  list: LinkedListAliasNode,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.AliasThatPointsToRecursiveType(
    list,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "AliasWithMultipleAttrs" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { Checked<number,"gt_ten"> } money - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const AliasWithMultipleAttrs = async (
  money: Checked<number,"gt_ten">,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.AliasWithMultipleAttrs(
    money,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "AliasedInputClass" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { InputClass } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const AliasedInputClass = async (
  input: InputClass,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.AliasedInputClass(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "AliasedInputClass2" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { InputClass } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const AliasedInputClass2 = async (
  input: InputClass,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.AliasedInputClass2(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "AliasedInputClassNested" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { InputClassNested } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const AliasedInputClassNested = async (
  input: InputClassNested,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.AliasedInputClassNested(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "AliasedInputEnum" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { AliasedEnum } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const AliasedInputEnum = async (
  input: AliasedEnum,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.AliasedInputEnum(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "AliasedInputList" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { AliasedEnum[] } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const AliasedInputList = async (
  input: AliasedEnum[],
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.AliasedInputList(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "AllowedOptionals" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { OptionalListAndMap } optionals - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const AllowedOptionals = async (
  optionals: OptionalListAndMap,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.AllowedOptionals(
    optionals,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "AssertFn" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { number } a - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const AssertFn = async (
  a: number,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.AssertFn(
    a,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "AudioInput" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { Audio } aud - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const AudioInput = async (
  aud: Audio,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.AudioInput(
    aud,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "BuildLinkedList" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { number[] } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const BuildLinkedList = async (
  input: number[],
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.BuildLinkedList(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "BuildTree" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { BinaryNode } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const BuildTree = async (
  input: BinaryNode,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.BuildTree(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "ClassThatPointsToRecursiveClassThroughAlias" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { ClassToRecAlias } cls - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const ClassThatPointsToRecursiveClassThroughAlias = async (
  cls: ClassToRecAlias,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.ClassThatPointsToRecursiveClassThroughAlias(
    cls,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "ClassifyDynEnumTwo" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const ClassifyDynEnumTwo = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.ClassifyDynEnumTwo(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "ClassifyMessage" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const ClassifyMessage = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.ClassifyMessage(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "ClassifyMessage2" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const ClassifyMessage2 = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.ClassifyMessage2(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "ClassifyMessage3" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const ClassifyMessage3 = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.ClassifyMessage3(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "Completion" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } prefix - Input parameter.
 * @param { string } suffix - Input parameter.
 * @param { string } language - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const Completion = async (
  prefix: string,
  suffix: string,
  language: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.Completion(
    prefix,
    suffix,
    language,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "CustomTask" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const CustomTask = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.CustomTask(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "DescribeImage" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { Image } img - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const DescribeImage = async (
  img: Image,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.DescribeImage(
    img,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "DescribeImage2" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { ClassWithImage } classWithImage - Input parameter.
 * @param { Image } img2 - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const DescribeImage2 = async (
  classWithImage: ClassWithImage,
  img2: Image,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.DescribeImage2(
    classWithImage,
    img2,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "DescribeImage3" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { ClassWithImage } classWithImage - Input parameter.
 * @param { Image } img2 - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const DescribeImage3 = async (
  classWithImage: ClassWithImage,
  img2: Image,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.DescribeImage3(
    classWithImage,
    img2,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "DescribeImage4" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { ClassWithImage } classWithImage - Input parameter.
 * @param { Image } img2 - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const DescribeImage4 = async (
  classWithImage: ClassWithImage,
  img2: Image,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.DescribeImage4(
    classWithImage,
    img2,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "DifferentiateUnions" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const DifferentiateUnions = async (
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.DifferentiateUnions(
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "DummyOutputFunction" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const DummyOutputFunction = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.DummyOutputFunction(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "DynamicFunc" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { DynamicClassOne } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const DynamicFunc = async (
  input: DynamicClassOne,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.DynamicFunc(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "DynamicInputOutput" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { DynInputOutput } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const DynamicInputOutput = async (
  input: DynInputOutput,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.DynamicInputOutput(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "DynamicListInputOutput" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { DynInputOutput[] } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const DynamicListInputOutput = async (
  input: DynInputOutput[],
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.DynamicListInputOutput(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "ExpectFailure" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const ExpectFailure = async (
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.ExpectFailure(
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "ExtractContactInfo" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } document - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const ExtractContactInfo = async (
  document: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.ExtractContactInfo(
    document,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "ExtractHobby" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } text - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const ExtractHobby = async (
  text: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.ExtractHobby(
    text,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "ExtractNames" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const ExtractNames = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.ExtractNames(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "ExtractPeople" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } text - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const ExtractPeople = async (
  text: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.ExtractPeople(
    text,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "ExtractReceiptInfo" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } email - Input parameter.
 * @param { "curiosity" | "personal_finance" } reason - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const ExtractReceiptInfo = async (
  email: string,
  reason: "curiosity" | "personal_finance",
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.ExtractReceiptInfo(
    email,
    reason,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "ExtractResume" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } resume - Input parameter.
 * @param { Image | null } img (optional) - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const ExtractResume = async (
  resume: string,
  img?: Image | null,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.ExtractResume(
    resume,
    img,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "ExtractResume2" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } resume - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const ExtractResume2 = async (
  resume: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.ExtractResume2(
    resume,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "FnClassOptionalOutput" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const FnClassOptionalOutput = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.FnClassOptionalOutput(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "FnClassOptionalOutput2" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const FnClassOptionalOutput2 = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.FnClassOptionalOutput2(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "FnEnumListOutput" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const FnEnumListOutput = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.FnEnumListOutput(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "FnEnumOutput" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const FnEnumOutput = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.FnEnumOutput(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "FnLiteralClassInputOutput" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { LiteralClassHello } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const FnLiteralClassInputOutput = async (
  input: LiteralClassHello,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.FnLiteralClassInputOutput(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "FnLiteralUnionClassInputOutput" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { LiteralClassOne | LiteralClassTwo } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const FnLiteralUnionClassInputOutput = async (
  input: LiteralClassOne | LiteralClassTwo,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.FnLiteralUnionClassInputOutput(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "FnNamedArgsSingleStringOptional" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string | null } myString (optional) - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const FnNamedArgsSingleStringOptional = async (
  myString?: string | null,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.FnNamedArgsSingleStringOptional(
    myString,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "FnOutputBool" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const FnOutputBool = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.FnOutputBool(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "FnOutputClass" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const FnOutputClass = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.FnOutputClass(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "FnOutputClassList" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const FnOutputClassList = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.FnOutputClassList(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "FnOutputClassNested" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const FnOutputClassNested = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.FnOutputClassNested(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "FnOutputClassWithEnum" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const FnOutputClassWithEnum = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.FnOutputClassWithEnum(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "FnOutputInt" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const FnOutputInt = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.FnOutputInt(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "FnOutputLiteralBool" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const FnOutputLiteralBool = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.FnOutputLiteralBool(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "FnOutputLiteralInt" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const FnOutputLiteralInt = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.FnOutputLiteralInt(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "FnOutputLiteralString" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const FnOutputLiteralString = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.FnOutputLiteralString(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "FnOutputStringList" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const FnOutputStringList = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.FnOutputStringList(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "FnTestAliasedEnumOutput" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const FnTestAliasedEnumOutput = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.FnTestAliasedEnumOutput(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "FnTestClassAlias" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const FnTestClassAlias = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.FnTestClassAlias(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "FnTestNamedArgsSingleEnum" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { NamedArgsSingleEnum } myArg - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const FnTestNamedArgsSingleEnum = async (
  myArg: NamedArgsSingleEnum,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.FnTestNamedArgsSingleEnum(
    myArg,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "GetDataType" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } text - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const GetDataType = async (
  text: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.GetDataType(
    text,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "GetOrderInfo" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { Email } email - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const GetOrderInfo = async (
  email: Email,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.GetOrderInfo(
    email,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "GetQuery" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } query - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const GetQuery = async (
  query: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.GetQuery(
    query,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "InOutEnumMapKey" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { Partial<Record<types.MapKey, string>> } i1 - Input parameter.
 * @param { Partial<Record<types.MapKey, string>> } i2 - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const InOutEnumMapKey = async (
  i1: Partial<Record<types.MapKey, string>>,
  i2: Partial<Record<types.MapKey, string>>,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.InOutEnumMapKey(
    i1,
    i2,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "InOutLiteralStringUnionMapKey" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { Partial<Record<"one" | "two" | "three" | "four", string>> } i1 - Input parameter.
 * @param { Partial<Record<"one" | "two" | "three" | "four", string>> } i2 - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const InOutLiteralStringUnionMapKey = async (
  i1: Partial<Record<"one" | "two" | "three" | "four", string>>,
  i2: Partial<Record<"one" | "two" | "three" | "four", string>>,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.InOutLiteralStringUnionMapKey(
    i1,
    i2,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "InOutSingleLiteralStringMapKey" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { Partial<Record<"key", string>> } m - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const InOutSingleLiteralStringMapKey = async (
  m: Partial<Record<"key", string>>,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.InOutSingleLiteralStringMapKey(
    m,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "JsonTypeAliasCycle" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { JsonValue } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const JsonTypeAliasCycle = async (
  input: JsonValue,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.JsonTypeAliasCycle(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "LiteralUnionsTest" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const LiteralUnionsTest = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.LiteralUnionsTest(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "MakeBlockConstraint" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const MakeBlockConstraint = async (
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.MakeBlockConstraint(
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "MakeNestedBlockConstraint" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const MakeNestedBlockConstraint = async (
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.MakeNestedBlockConstraint(
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "MakeSemanticContainer" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const MakeSemanticContainer = async (
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.MakeSemanticContainer(
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "MapAlias" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { Record<string, string[]> } m - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const MapAlias = async (
  m: Record<string, string[]>,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.MapAlias(
    m,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "MergeAliasAttributes" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { number } money - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const MergeAliasAttributes = async (
  money: number,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.MergeAliasAttributes(
    money,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "MyFunc" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const MyFunc = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.MyFunc(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "NestedAlias" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { number | string | boolean | number | string[] | Record<string, string[]> } c - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const NestedAlias = async (
  c: number | string | boolean | number | string[] | Record<string, string[]>,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.NestedAlias(
    c,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "NullLiteralClassHello" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } s - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const NullLiteralClassHello = async (
  s: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.NullLiteralClassHello(
    s,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "OptionalTest_Function" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const OptionalTest_Function = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.OptionalTest_Function(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "PredictAge" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } name - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const PredictAge = async (
  name: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.PredictAge(
    name,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "PredictAgeBare" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } inp - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const PredictAgeBare = async (
  inp: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.PredictAgeBare(
    inp,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "PrimitiveAlias" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { number | string | boolean | number } p - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const PrimitiveAlias = async (
  p: number | string | boolean | number,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.PrimitiveAlias(
    p,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "PromptTestClaude" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const PromptTestClaude = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.PromptTestClaude(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "PromptTestClaudeChat" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const PromptTestClaudeChat = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.PromptTestClaudeChat(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "PromptTestClaudeChatNoSystem" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const PromptTestClaudeChatNoSystem = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.PromptTestClaudeChatNoSystem(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "PromptTestOpenAI" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const PromptTestOpenAI = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.PromptTestOpenAI(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "PromptTestOpenAIChat" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const PromptTestOpenAIChat = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.PromptTestOpenAIChat(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "PromptTestOpenAIChatNoSystem" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const PromptTestOpenAIChatNoSystem = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.PromptTestOpenAIChatNoSystem(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "PromptTestStreaming" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const PromptTestStreaming = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.PromptTestStreaming(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "RecursiveAliasCycle" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { RecAliasOne } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const RecursiveAliasCycle = async (
  input: RecAliasOne,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.RecursiveAliasCycle(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "RecursiveClassWithAliasIndirection" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { NodeWithAliasIndirection } cls - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const RecursiveClassWithAliasIndirection = async (
  cls: NodeWithAliasIndirection,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.RecursiveClassWithAliasIndirection(
    cls,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "ReturnAliasWithMergedAttributes" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { Checked<number,"gt_ten"> } money - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const ReturnAliasWithMergedAttributes = async (
  money: Checked<number,"gt_ten">,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.ReturnAliasWithMergedAttributes(
    money,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "ReturnFailingAssert" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { number } inp - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const ReturnFailingAssert = async (
  inp: number,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.ReturnFailingAssert(
    inp,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "ReturnJsonEntry" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } s - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const ReturnJsonEntry = async (
  s: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.ReturnJsonEntry(
    s,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "ReturnMalformedConstraints" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { number } a - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const ReturnMalformedConstraints = async (
  a: number,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.ReturnMalformedConstraints(
    a,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "SchemaDescriptions" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const SchemaDescriptions = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.SchemaDescriptions(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "SimpleRecursiveListAlias" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { RecursiveListAlias } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const SimpleRecursiveListAlias = async (
  input: RecursiveListAlias,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.SimpleRecursiveListAlias(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "SimpleRecursiveMapAlias" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { RecursiveMapAlias } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const SimpleRecursiveMapAlias = async (
  input: RecursiveMapAlias,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.SimpleRecursiveMapAlias(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "StreamBigNumbers" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { number } digits - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const StreamBigNumbers = async (
  digits: number,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.StreamBigNumbers(
    digits,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "StreamFailingAssertion" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } theme - Input parameter.
 * @param { number } length - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const StreamFailingAssertion = async (
  theme: string,
  length: number,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.StreamFailingAssertion(
    theme,
    length,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "StreamOneBigNumber" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { number } digits - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const StreamOneBigNumber = async (
  digits: number,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.StreamOneBigNumber(
    digits,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "StreamUnionIntegers" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { number } digits - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const StreamUnionIntegers = async (
  digits: number,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.StreamUnionIntegers(
    digits,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "StreamingCompoundNumbers" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { number } digits - Input parameter.
 * @param { boolean } yapping - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const StreamingCompoundNumbers = async (
  digits: number,
  yapping: boolean,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.StreamingCompoundNumbers(
    digits,
    yapping,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TakeRecAliasDep" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { RecursiveAliasDependency } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TakeRecAliasDep = async (
  input: RecursiveAliasDependency,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TakeRecAliasDep(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestAnthropic" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestAnthropic = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestAnthropic(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestAnthropicShorthand" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestAnthropicShorthand = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestAnthropicShorthand(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestAws" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestAws = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestAws(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestAwsInvalidAccessKey" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestAwsInvalidAccessKey = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestAwsInvalidAccessKey(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestAwsInvalidProfile" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestAwsInvalidProfile = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestAwsInvalidProfile(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestAwsInvalidRegion" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestAwsInvalidRegion = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestAwsInvalidRegion(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestAwsInvalidSessionToken" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestAwsInvalidSessionToken = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestAwsInvalidSessionToken(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestAzure" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestAzure = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestAzure(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestAzureFailure" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestAzureFailure = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestAzureFailure(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestAzureO1NoMaxTokens" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestAzureO1NoMaxTokens = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestAzureO1NoMaxTokens(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestAzureO1WithMaxCompletionTokens" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestAzureO1WithMaxCompletionTokens = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestAzureO1WithMaxCompletionTokens(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestAzureO1WithMaxTokens" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestAzureO1WithMaxTokens = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestAzureO1WithMaxTokens(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestAzureO3NoMaxTokens" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestAzureO3NoMaxTokens = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestAzureO3NoMaxTokens(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestAzureO3WithMaxCompletionTokens" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestAzureO3WithMaxCompletionTokens = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestAzureO3WithMaxCompletionTokens(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestAzureWithMaxTokens" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestAzureWithMaxTokens = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestAzureWithMaxTokens(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestCaching" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 * @param { string } not_cached - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestCaching = async (
  input: string,
  not_cached: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestCaching(
    input,
    not_cached,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestFallbackClient" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestFallbackClient = async (
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestFallbackClient(
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestFallbackToShorthand" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestFallbackToShorthand = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestFallbackToShorthand(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestFnNamedArgsSingleBool" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { boolean } myBool - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestFnNamedArgsSingleBool = async (
  myBool: boolean,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestFnNamedArgsSingleBool(
    myBool,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestFnNamedArgsSingleClass" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { NamedArgsSingleClass } myArg - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestFnNamedArgsSingleClass = async (
  myArg: NamedArgsSingleClass,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestFnNamedArgsSingleClass(
    myArg,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestFnNamedArgsSingleEnumList" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { NamedArgsSingleEnumList[] } myArg - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestFnNamedArgsSingleEnumList = async (
  myArg: NamedArgsSingleEnumList[],
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestFnNamedArgsSingleEnumList(
    myArg,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestFnNamedArgsSingleFloat" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { number } myFloat - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestFnNamedArgsSingleFloat = async (
  myFloat: number,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestFnNamedArgsSingleFloat(
    myFloat,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestFnNamedArgsSingleInt" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { number } myInt - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestFnNamedArgsSingleInt = async (
  myInt: number,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestFnNamedArgsSingleInt(
    myInt,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestFnNamedArgsSingleMapStringToClass" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { Record<string, StringToClassEntry> } myMap - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestFnNamedArgsSingleMapStringToClass = async (
  myMap: Record<string, StringToClassEntry>,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestFnNamedArgsSingleMapStringToClass(
    myMap,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestFnNamedArgsSingleMapStringToMap" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { Record<string, Record<string, string>> } myMap - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestFnNamedArgsSingleMapStringToMap = async (
  myMap: Record<string, Record<string, string>>,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestFnNamedArgsSingleMapStringToMap(
    myMap,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestFnNamedArgsSingleMapStringToString" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { Record<string, string> } myMap - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestFnNamedArgsSingleMapStringToString = async (
  myMap: Record<string, string>,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestFnNamedArgsSingleMapStringToString(
    myMap,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestFnNamedArgsSingleString" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } myString - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestFnNamedArgsSingleString = async (
  myString: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestFnNamedArgsSingleString(
    myString,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestFnNamedArgsSingleStringArray" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string[] } myStringArray - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestFnNamedArgsSingleStringArray = async (
  myStringArray: string[],
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestFnNamedArgsSingleStringArray(
    myStringArray,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestFnNamedArgsSingleStringList" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string[] } myArg - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestFnNamedArgsSingleStringList = async (
  myArg: string[],
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestFnNamedArgsSingleStringList(
    myArg,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestGemini" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestGemini = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestGemini(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestGeminiOpenAiGeneric" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestGeminiOpenAiGeneric = async (
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestGeminiOpenAiGeneric(
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestGeminiSystem" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestGeminiSystem = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestGeminiSystem(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestGeminiSystemAsChat" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestGeminiSystemAsChat = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestGeminiSystemAsChat(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestImageInput" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { Image } img - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestImageInput = async (
  img: Image,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestImageInput(
    img,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestImageInputAnthropic" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { Image } img - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestImageInputAnthropic = async (
  img: Image,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestImageInputAnthropic(
    img,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestImageListInput" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { Image[] } imgs - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestImageListInput = async (
  imgs: Image[],
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestImageListInput(
    imgs,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestMemory" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestMemory = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestMemory(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestMulticlassNamedArgs" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { NamedArgsSingleClass } myArg - Input parameter.
 * @param { NamedArgsSingleClass } myArg2 - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestMulticlassNamedArgs = async (
  myArg: NamedArgsSingleClass,
  myArg2: NamedArgsSingleClass,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestMulticlassNamedArgs(
    myArg,
    myArg2,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestNamedArgsLiteralBool" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { true } myBool - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestNamedArgsLiteralBool = async (
  myBool: true,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestNamedArgsLiteralBool(
    myBool,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestNamedArgsLiteralInt" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { 1 } myInt - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestNamedArgsLiteralInt = async (
  myInt: 1,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestNamedArgsLiteralInt(
    myInt,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestNamedArgsLiteralString" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { "My String" } myString - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestNamedArgsLiteralString = async (
  myString: "My String",
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestNamedArgsLiteralString(
    myString,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestOllama" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestOllama = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestOllama(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestOpenAI" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestOpenAI = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestOpenAI(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestOpenAILegacyProvider" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestOpenAILegacyProvider = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestOpenAILegacyProvider(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestOpenAIO1NoMaxTokens" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestOpenAIO1NoMaxTokens = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestOpenAIO1NoMaxTokens(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestOpenAIO1WithMaxCompletionTokens" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestOpenAIO1WithMaxCompletionTokens = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestOpenAIO1WithMaxCompletionTokens(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestOpenAIO1WithMaxTokens" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestOpenAIO1WithMaxTokens = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestOpenAIO1WithMaxTokens(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestOpenAIShorthand" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestOpenAIShorthand = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestOpenAIShorthand(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestOpenAIWithMaxTokens" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestOpenAIWithMaxTokens = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestOpenAIWithMaxTokens(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestOpenAIWithNullMaxTokens" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestOpenAIWithNullMaxTokens = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestOpenAIWithNullMaxTokens(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestRetryConstant" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestRetryConstant = async (
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestRetryConstant(
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestRetryExponential" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestRetryExponential = async (
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestRetryExponential(
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestSingleFallbackClient" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestSingleFallbackClient = async (
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestSingleFallbackClient(
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestUniverseQuestion" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { UniverseQuestionInput } question - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestUniverseQuestion = async (
  question: UniverseQuestionInput,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestUniverseQuestion(
    question,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestVertex" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestVertex = async (
  input: string,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestVertex(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "TestVertexWithSystemInstructions" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const TestVertexWithSystemInstructions = async (
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.TestVertexWithSystemInstructions(
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "UnionTest_Function" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { string | boolean } input - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const UnionTest_Function = async (
  input: string | boolean,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.UnionTest_Function(
    input,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "UseBlockConstraint" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { BlockConstraintForParam } inp - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const UseBlockConstraint = async (
  inp: BlockConstraintForParam,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.UseBlockConstraint(
    inp,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "UseMalformedConstraints" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { MalformedConstraints2 } a - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const UseMalformedConstraints = async (
  a: MalformedConstraints2,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.UseMalformedConstraints(
    a,
  );
  return Promise.resolve(stream.toStreamable());
};

/**
 * Executes the streaming variant of the "UseNestedBlockConstraint" BAML action.
 *
 * This action initiates a streaming response by calling the corresponding
 * BAML stream function. The returned stream yields incremental updates.
 *
 * @param { NestedBlockConstraintForParam } inp - Input parameter.
 *
 * @returns {ReadableStream<Uint8Array>} A stream that yields incremental updates from the action.
 */
export const UseNestedBlockConstraint = async (
  inp: NestedBlockConstraintForParam,
): Promise<ReadableStream<Uint8Array>> => {
  const stream = b.stream.UseNestedBlockConstraint(
    inp,
  );
  return Promise.resolve(stream.toStreamable());
};