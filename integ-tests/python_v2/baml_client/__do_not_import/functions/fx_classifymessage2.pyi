# This file is generated by the BAML compiler.
# Do not edit this file directly.
# Instead, edit the BAML files and recompile.

# ruff: noqa: E501,F401
# flake8: noqa: E501,F401
# pylint: disable=unused-import,line-too-long
# fmt: off

from ..types.enums.enm_category import Category
from baml_core.stream import AsyncStream
from typing import Callable, Protocol, runtime_checkable


import typing

import pytest
from contextlib import contextmanager
from unittest import mock

ImplName = typing.Literal["default_config"]

T = typing.TypeVar("T", bound=typing.Callable[..., typing.Any])
CLS = typing.TypeVar("CLS", bound=type)


IClassifyMessage2Output = Category

@runtime_checkable
class IClassifyMessage2(Protocol):
    """
    This is the interface for a function.

    Args:
        input: str

    Returns:
        Category
    """

    async def __call__(self, *, input: str) -> Category:
        ...

   

@runtime_checkable
class IClassifyMessage2Stream(Protocol):
    """
    This is the interface for a stream function.

    Args:
        input: str

    Returns:
        AsyncStream[Category, Category]
    """

    def __call__(self, *, input: str
) -> AsyncStream[Category, Category]:
        ...
class BAMLClassifyMessage2Impl:
    async def run(self, *, input: str) -> Category:
        ...
    
    def stream(self, *, input: str
) -> AsyncStream[Category, Category]:
        ...

class IBAMLClassifyMessage2:
    def register_impl(
        self, name: ImplName
    ) -> typing.Callable[[IClassifyMessage2, IClassifyMessage2Stream], None]:
        ...

    async def __call__(self, *, input: str) -> Category:
        ...

    def stream(self, *, input: str
) -> AsyncStream[Category, Category]:
        ...

    def get_impl(self, name: ImplName) -> BAMLClassifyMessage2Impl:
        ...

    @contextmanager
    def mock(self) -> typing.Generator[mock.AsyncMock, None, None]:
        """
        Utility for mocking the ClassifyMessage2Interface.

        Usage:
            ```python
            # All implementations are mocked.

            async def test_logic() -> None:
                with baml.ClassifyMessage2.mock() as mocked:
                    mocked.return_value = ...
                    result = await ClassifyMessage2Impl(...)
                    assert mocked.called
            ```
        """
        ...

    @typing.overload
    def test(self, test_function: T) -> T:
        """
        Provides a pytest.mark.parametrize decorator to facilitate testing different implementations of
        the ClassifyMessage2Interface.

        Args:
            test_function : T
                The test function to be decorated.

        Usage:
            ```python
            # All implementations will be tested.

            @baml.ClassifyMessage2.test
            async def test_logic(ClassifyMessage2Impl: IClassifyMessage2) -> None:
                result = await ClassifyMessage2Impl(...)
            ```
        """
        ...

    @typing.overload
    def test(self, *, exclude_impl: typing.Iterable[ImplName] = [], stream: bool = False) -> pytest.MarkDecorator:
        """
        Provides a pytest.mark.parametrize decorator to facilitate testing different implementations of
        the ClassifyMessage2Interface.

        Args:
            exclude_impl : Iterable[ImplName]
                The names of the implementations to exclude from testing.
            stream: bool
                If set, will return a streamable version of the test function.

        Usage:
            ```python
            # All implementations except the given impl will be tested.

            @baml.ClassifyMessage2.test(exclude_impl=["implname"])
            async def test_logic(ClassifyMessage2Impl: IClassifyMessage2) -> None:
                result = await ClassifyMessage2Impl(...)
            ```

            ```python
            # Streamable version of the test function.

            @baml.ClassifyMessage2.test(stream=True)
            async def test_logic(ClassifyMessage2Impl: IClassifyMessage2Stream) -> None:
                async for result in ClassifyMessage2Impl(...):
                    ...
            ```
        """
        ...

    @typing.overload
    def test(self, test_class: typing.Type[CLS]) -> typing.Type[CLS]:
        """
        Provides a pytest.mark.parametrize decorator to facilitate testing different implementations of
        the ClassifyMessage2Interface.

        Args:
            test_class : Type[CLS]
                The test class to be decorated.

        Usage:
        ```python
        # All implementations will be tested in every test method.

        @baml.ClassifyMessage2.test
        class TestClass:
            def test_a(self, ClassifyMessage2Impl: IClassifyMessage2) -> None:
                ...
            def test_b(self, ClassifyMessage2Impl: IClassifyMessage2) -> None:
                ...
        ```
        """
        ...

BAMLClassifyMessage2: IBAMLClassifyMessage2
