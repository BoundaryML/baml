# BAML generated-code contract probe

This repository-only package exists to prove that generated C# in an arbitrary
consumer assembly can use a narrow, versioned, reflection-free runtime
contract. It is not a public BAML package.
