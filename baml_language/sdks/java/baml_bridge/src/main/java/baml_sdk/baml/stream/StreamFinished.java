package baml_sdk.baml.stream;

/**
 * Runtime-owned stream sentinel (BAML stdlib path {@code baml.stream}).
 * Compile-only stub. Split-package with generated {@code baml_sdk.*} code on
 * the classpath is expected (the runtime owns this path per the conventions
 * doc). Peer of {@code baml_bridge.StreamFinished}; the permanent home is TBD.
 */
public final class StreamFinished {
    public StreamFinished() {}
}
