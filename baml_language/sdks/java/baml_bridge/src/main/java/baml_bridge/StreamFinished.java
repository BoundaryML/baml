package baml_bridge;

/**
 * Sentinel signalling that a {@link BamlStream} has produced its final value
 * (the "finished" arm of {@code next()}). Compile-only stub. Its permanent home
 * (here vs. {@code baml_sdk.baml.stream}) is still open per the conventions doc;
 * both are provided for now.
 */
public final class StreamFinished {
    public StreamFinished() {}
}
