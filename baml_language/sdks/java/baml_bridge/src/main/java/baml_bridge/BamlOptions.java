package baml_bridge;

/**
 * Per-call options (the trailing configurator object for optional args, call
 * context, collectors, etc.). Compile-only stub — no options are wired yet.
 */
public final class BamlOptions {
    public BamlOptions() {}
}
