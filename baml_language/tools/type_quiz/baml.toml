[package]
name = "type_quiz"

# `default` is what a bare `baml-cli test` (and CI) runs: everything except the
# opt-in `::live::` testsets, which call a real model to calibrate the grader.
[test]
default = "offline"

[test.profiles.offline]
args = ["-x", "::live::"]

[test.profiles.live]
args = ["-i", "::live::"]

# The quiz's user interface is a browser page, so the package is generated as
# a web SDK. This section says what to generate; the app that consumes it says
# where to put it, by passing `--output-dir`, so this package holds no path
# into the front end.
[generator.web]
output_type = "typescript/web"
naming_convention = "preserve-case"
