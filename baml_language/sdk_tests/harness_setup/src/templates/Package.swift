// swift-tools-version: 6.1
// Generated by sdk_test_harness_setup::swift — do not edit.
//
// Per-fixture SwiftPM package. `Sources/Baml/` is sdkgen_swift output;
// `Tests/BamlTests/` is the copied `customizable/` overlay. The BamlBridge
// runtime resolves via a path dependency 5 ancestors up
// (crates/swift/<F>/generated/ -> … -> baml_language/) to `sdks/swift`,
// whose Binaries/BamlBridgeFFI.xcframework is built by
// `crates/swift/setup.sh` (fired by `cargo nextest run`).
import PackageDescription

let package = Package(
    name: "__PACKAGE_NAME__",
    platforms: [
        .macOS(.v13)
    ],
    dependencies: [
        .package(path: "../../../../../sdks/swift")
    ],
    targets: [
        .target(
            name: "Baml",
            dependencies: [
                // Package identity of a path dependency is its directory
                // name (`sdks/swift`), not its declared package name.
                .product(name: "BamlBridge", package: "swift")
            ],
            path: "Sources/Baml"
        ),
        .testTarget(
            name: "BamlTests",
            dependencies: ["Baml"],
            path: "Tests/BamlTests"
        ),
    ]
)
