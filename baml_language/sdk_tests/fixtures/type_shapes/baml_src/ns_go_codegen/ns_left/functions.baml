// The Go SDK routes this function to baml_sdk/go_codegen/left. Its sibling
// deliberately uses the same bare name to prove namespaces do not collide.
function echo(value: string) -> string {
  value
}
