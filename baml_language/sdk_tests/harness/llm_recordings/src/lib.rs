//! `sdk_test_llm_recordings` — the streaming replay-harness recorder.
//!
//! All logic lives in the integration test `tests/recordings.rs`; this lib is
//! intentionally empty (it only exists so the crate has a target). See the
//! crate `Cargo.toml` and
//! `sdk_tests/fixtures/llm_functions/recordings/README.md` for the design.
