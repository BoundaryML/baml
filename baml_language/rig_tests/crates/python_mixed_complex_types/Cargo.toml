# Hand-written rig crate (was `tools_rig`-generated; now modeled on
# `python_example_09a`). Drives codegen from real `.baml` source under
# `baml_src/` through the full `baml_project::build_symbol_pool` pipeline.
[package]
name = "rig_python_mixed_complex_types"
version = "0.1.0"
edition = "2021"
publish = false

[build-dependencies]
baml_codegen_python = { path = "../../../crates/baml_codegen_python" }
baml_codegen_types = { path = "../../../crates/baml_codegen_types" }
baml_db = { path = "../../../crates/baml_db" }
baml_project = { path = "../../../crates/baml_project" }
baml_workspace = { path = "../../../crates/baml_workspace" }

[package.metadata.ci]
wasm_support = false
