[package]
name = "claude_code"
# Implicitly available to every package (no `[dependencies]` entry needed).
prelude = true

[dependencies]
baml = { path = "../baml" }
log = { path = "../log" }
ai = { path = "../ai" }
