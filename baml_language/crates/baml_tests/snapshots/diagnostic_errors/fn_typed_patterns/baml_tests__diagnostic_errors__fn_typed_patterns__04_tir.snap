---
source: crates/baml_tests/src/generated_tests.rs
---
=== TIR2 ===
function user.dispatch_callback(cb: ((int) -> int throws never) | ((string) -> string throws never)) -> int throws never {
  { : int | 0
    match (cb : ((int) -> int throws never) | ((string) -> string throws never)) : int | 0
      f: (int) -> int throws never =>
        f(0) : int
      (string) -> string throws never =>
        0 : 0
  }
  !! 1093..1118: cannot test a value against function type `(int) -> int throws never` at runtime: callable signatures cannot yet be faithfully reconstructed for every value (bound methods, closures from generic frames), so this test would silently misroute them; make it the final arm of an exhaustive `match` (which needs no test) or match on a non-function discriminant
}
function user.unwrap_typed<T>(cb: ((int) -> T throws never) | ((string) -> T throws never)) -> T throws never {
  { : T
    match (cb : ((int) -> T throws never) | ((string) -> T throws never)) : T
      f: (int) -> T throws never =>
        f<T>(0) : T
      f: (string) -> T throws never =>
        f<T>("") : T
  }
  !! 1406..1429: cannot test a value against function type `(int) -> T throws never` at runtime: callable signatures cannot yet be faithfully reconstructed for every value (bound methods, closures from generic frames), so this test would silently misroute them; make it the final arm of an exhaustive `match` (which needs no test) or match on a non-function discriminant
}
function user.dispatch_with_wildcard(cb: ((int) -> int throws never) | ((string) -> int throws never)) -> int throws never {
  { : 1 | 0
    match (cb : ((int) -> int throws never) | ((string) -> int throws never)) : 1 | 0
      (int) -> int throws never =>
        1 : 1
      _ =>
        0 : 0
  }
  !! 1776..1801: cannot test a value against function type `(int) -> int throws never` at runtime: callable signatures cannot yet be faithfully reconstructed for every value (bound methods, closures from generic frames), so this test would silently misroute them; make it the final arm of an exhaustive `match` (which needs no test) or match on a non-function discriminant
}
function user.fn_arm_not_last(cb: ((int) -> int throws never) | null) -> int throws never {
  { : int | 0
    match (cb : ((int) -> int throws never) | null) : int | 0
      f: (int) -> int throws never =>
        f(1) : int
      null =>
        0 : 0
  }
  !! 2083..2108: cannot test a value against function type `(int) -> int throws never` at runtime: callable signatures cannot yet be faithfully reconstructed for every value (bound methods, closures from generic frames), so this test would silently misroute them; make it the final arm of an exhaustive `match` (which needs no test) or match on a non-function discriminant
}
function user.fn_arm_guarded(cb: (int) -> int throws never) -> int throws never {
  { : int
    match (cb : (int) -> int throws never) : int
      f: (int) -> int throws never if true =>
        f(1) : int
  }
  !! 2360..2435: non-exhaustive match on `(int) -> int throws never`; missing: _
  !! 2388..2413: cannot test a value against function type `(int) -> int throws never` at runtime: callable signatures cannot yet be faithfully reconstructed for every value (bound methods, closures from generic frames), so this test would silently misroute them; make it the final arm of an exhaustive `match` (which needs no test) or match on a non-function discriminant
}
function user.fn_union_member_arm(v: int | ((int) -> int throws never) | string) -> string throws never {
  { : "either" | string
    match (v : int | ((int) -> int throws never) | string) : "either" | string
      x: (int) -> int throws never | int =>
        "either" : "either"
      s: string =>
        s : string
  }
  !! 2682..2707: cannot test a value against function type `(int) -> int throws never` at runtime: callable signatures cannot yet be faithfully reconstructed for every value (bound methods, closures from generic frames), so this test would silently misroute them; make it the final arm of an exhaustive `match` (which needs no test) or match on a non-function discriminant
}
function user.fn_is(cb: (int) -> int throws never) -> bool throws never {
  { : bool
    cb is (int) -> int throws unknown : bool
  }
  !! 2970..2997: cannot test a value against function type `(int) -> int throws never` at runtime: callable signatures cannot yet be faithfully reconstructed for every value (bound methods, closures from generic frames), so this test would silently misroute them; make it the final arm of an exhaustive `match` (which needs no test) or match on a non-function discriminant
}
function user.fn_if_let(cb: (int) -> int throws never) -> int throws never {
  { : int | 0
    if let <pat> = cb { 0 stmts + tail } else { 0 stmts + tail } : int | 0
  }
  ?? 3106..3140: irrefutable `if let` pattern; the `else` branch is unreachable — use a plain `let` binding instead
  !! 3113..3140: cannot test a value against function type `(int) -> int throws never` at runtime: callable signatures cannot yet be faithfully reconstructed for every value (bound methods, closures from generic frames), so this test would silently misroute them; make it the final arm of an exhaustive `match` (which needs no test) or match on a non-function discriminant
}
