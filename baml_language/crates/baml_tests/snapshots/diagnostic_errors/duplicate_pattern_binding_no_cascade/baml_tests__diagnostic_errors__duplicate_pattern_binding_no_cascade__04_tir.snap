---
source: crates/baml_tests/src/generated_tests.rs
---
=== TIR2 ===
class user.Person {
  age: int
  name: string
}
function user.string_last(person: user.Person) -> int throws never {
  { : !error
    let Person { age: item, name: item } = person : user.Person
    item : !error
  }
}
function user.int_last(person: user.Person) -> string throws never {
  { : !error
    let Person { name: item, age: item } = person : user.Person
    item : !error
  }
}
function user.if_let_string_last(person: user.Person | null) -> int throws never {
  { : !error | 0
    if let <pat> = person { 0 stmts + tail } else { 0 stmts + tail } : !error | 0
  }
}
function user.if_let_int_last(person: user.Person | null) -> string throws never {
  { : !error | ""
    if let <pat> = person { 0 stmts + tail } else { 0 stmts + tail } : !error | ""
  }
}
class user.Person$stream {
  age: int | null
  name: string | null
}
