function arith(a: int, b: int, x: float, s: string) -> string {
    let sum = a + b;
     // ^^^ int
    let mixed = a * x;
     // ^^^^^ float
    let neg = -a;
     // ^^^ int
    s + s
 // ^^^^^ string
}
