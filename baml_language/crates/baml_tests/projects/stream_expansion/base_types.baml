// Shared type definitions used by other files in this test project.
// Named "base_types" so it sorts alphabetically before other files.

enum Status {
    Active
    Inactive
}

class Education {
    school string
    degree string
}
