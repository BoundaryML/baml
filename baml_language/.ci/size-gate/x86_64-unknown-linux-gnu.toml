# Needs re-record: the size gate now tracks `baml-cli` and
# `packed-program` (was `bridge_cffi`). Run on a Linux x86_64 host:
#   mise run size-gate-record
# then commit the regenerated artifact entries below.
version = 1
recorded_at = "1970-01-01T00:00:00Z"
git_sha = ""
